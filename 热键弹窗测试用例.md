# 热键弹窗程序 —— 测试用例

> 配套程序：`popup_trainer.py`（尚未实现，见 `热键弹窗设计.md`）。
> 测试从外部使用者视角进行。`[自动]` 脚本可全自动判绿；`[手动]` 依赖真实按键 / 窗口可见性 / 音频，由人操作确认。
>
> **与主程序测试的区别**：弹窗程序的核心是"全局热键 + 控制台窗口显隐"，无法用 `conftest.py` 的 stdin/stdout 管道测试（管道子进程无真实控制台窗口，热键也无法靠写 stdin 触发）。因此自动测试走 **Win32 API 级**：直接调用 `RegisterHotKey`/`PostMessage`/`IsWindowVisible` 验证机制；物理按键走手动。

---

## 测试环境

- Windows，已装 pywin32（`win32com` 在用，`win32gui`/`win32con` 同包可用）
- 一台实体键盘（手动用例需真实按键）
- 默认控制台主机为 conhost（若为 Windows Terminal，部分窗口操作用例需人工确认）

---

## 实施侧约定（为可测试性，实现须满足）

`popup_trainer.py` 须暴露以下可观测点，自动测试才能成立：

   1. **状态日志**：关键事件写 stdout，前缀 `[POPUP]`，便于测试脚本解析：
      - `[POPUP] hotkey registered Ctrl+Shift+A`（注册成功）
      - `[POPUP] hotkey failed`（注册失败）
      - `[POPUP] hwnd=0x12345`（控制台窗口句柄）
      - `[POPUP] toggle show` / `[POPUP] toggle hide`
2. **测试钩子**：读环境变量 `POPUP_TEST=1` 时，启动后**不自动隐藏**控制台（便于测试观察显隐），且不阻塞过快。
3. **函数可单测**：`register_hotkey`、`get_console_hwnd`、`toggle(hwnd)` 为模块级函数，返回可断言的值（bool/int），供单元测试直接 import 调用。

---

## 一、热键机制（核心 —— "热键是否有效"）

### PTC-01：全局热键注册成功 `[自动]`

- **测试内容**：`Ctrl+Shift+A` 能否成功注册为全局热键
- **测试目的**：验证热键 API 可用、组合未被系统占用
- **测试过程**：
  1. 脚本创建一个隐藏消息窗口
  2. 调 `user32.RegisterHotKey(hwnd, id, MOD_CONTROL|MOD_SHIFT, VK_A)`
- **预期结果**：
  - 返回非 0（成功）
  - 组合未被系统占用
- **自动化说明**：纯 API 调用 + 返回值断言。注册后立即 `UnregisterHotKey` 释放。此用例直接回答"热键是否有效"的第一层。

### PTC-02：物理按键触发热键 `[自动·易抖动]`

- **测试内容**：真实按下 `Ctrl+Shift+A` 时，系统是否向注册窗口投递 `WM_HOTKEY`
- **测试目的**：端到端验证热键被系统捕获（非仅 API 返回成功）
- **测试过程**：
  1. 脚本注册热键 + 启动消息泵（2.5s 超时）
  2. 主线程用 `SendInput` 合成 `Ctrl↓ Shift↓ A↓ A↑ Shift↑ Ctrl↑`
  3. 消息泵线程检查是否收到 `WM_HOTKEY`(0x0312)
- **预期结果**：
  - 2.5s 内收到 `WM_HOTKEY`
- **自动化说明**：用 `SendInput` 模拟真实硬件事件，是"热键是否有效"的最强证明。**注**：`SendInput` 触发已注册热键偶有抖动（输入法、焦点状态影响）；若失败，回退以 PTC-01 + PTC-03 通过为准，并在报告标注 WARN。

### PTC-03：消息分发路径 `[自动]`

- **测试内容**：`WM_HOTKEY` 投递到窗口后，切换处理逻辑是否被触发
- **测试目的**：验证"收到热键消息 → 执行 toggle"的接线，不依赖物理按键
- **测试过程**：
  1. 脚本注册热键并启动消息泵
  2. 用 `PostMessage(hwnd, WM_HOTKEY, id, ...)` 直接投递消息（模拟 OS 投递）
  3. 消息泵收到后调用 toggle 处理，记日志
- **预期结果**：
  - 收到 `WM_HOTKEY`，toggle 被调用一次
- **自动化说明**：绕开物理按键的不确定性，隔离测试消息处理逻辑。`PostMessage` 投递的 `WM_HOTKEY` 与 OS 投递的等价。

### PTC-04：热键占用时注册失败 `[自动]`

- **测试内容**：主组合被占用时，是否正确报告失败
- **测试目的**：验证占用检测逻辑
- **测试过程**：
  1. 脚本 A 先注册 `Ctrl+Shift+A`（占住）
  2. 启动 `popup_trainer.py`（子进程）
  3. 读子进程 stdout
- **预期结果**：
  - stdout 出现 `[POPUP] hotkey failed`
  - 程序不崩溃，继续运行
- **自动化说明**：两个进程配合。脚本 A 占住主组合，弹窗程序应检测到注册失败并回退。

---

## 二、控制台窗口管理

### PTC-05：控制台窗口句柄获取 `[自动]`

- **测试内容**：`GetConsoleWindow()` 能否拿到本进程控制台句柄
- **测试目的**：验证窗口操作的前提（有句柄才能显隐）
- **测试过程**：
  1. 以 `CREATE_NEW_CONSOLE` 启动 `popup_trainer.py`（带真实控制台）
  2. 读 stdout 中的 `[POPUP] hwnd=0x...`
- **预期结果**：
  - hwnd 非 0
- **自动化说明**：必须用 `CREATE_NEW_CONSOLE` 启动（管道子进程无控制台，GetConsoleWindow 返回 0）。解析 stdout 日志断言。

### PTC-06：显隐切换 `[自动]`

- **测试内容**：toggle 能否切换控制台可见性
- **测试目的**：验证 ShowWindow 显隐逻辑
- **测试过程**：
  1. `CREATE_NEW_CONSOLE` 启动程序，拿到 hwnd
  2. 程序初始隐藏（POPUP_TEST 模式下不自动隐藏，故初始可见）
  3. 测试脚本 `PostMessage(hwnd, WM_HOTKEY, ...)` 触发 toggle
  4. 轮询 `win32gui.IsWindowVisible(hwnd)`
- **预期结果**：
  - 触发前可见 → 触发后不可见；再触发 → 可见
- **自动化说明**：用 PostMessage 触发真实 toggle 处理，用 IsWindowVisible 跨进程观测窗口状态。

### PTC-07：启动即隐藏 `[自动]`

- **测试内容**：正常模式（非 POPUP_TEST）启动后控制台是否自动隐藏
- **测试目的**：验证隐蔽常驻
- **测试过程**：
  1. `CREATE_NEW_CONSOLE` 启动程序（不加 POPUP_TEST）
  2. 等 1s
  3. 查 `IsWindowVisible(hwnd)`
- **预期结果**：
  - 窗口不可见
- **自动化说明**：需从 stdout 拿到 hwnd（日志在隐藏前已打印）。

---

## 三、main 集成

### PTC-08：main.main() 正常运行 `[自动]`

- **测试内容**：弹窗程序内 `main.main()` 能否跑起来，教材选择菜单出现
- **测试目的**：验证 import main + 调用入口无异常
- **测试过程**：
  1. `CREATE_NEW_CONSOLE` 启动程序（POPUP_TEST 模式，窗口可见）
  2. 程序 stdout 经日志或控制台输出捕获
  3. 等待"西班牙语陪练"标题出现
- **预期结果**：
  - 出现教材选择/主菜单文字
  - 程序不崩溃
- **自动化说明**：CREATE_NEW_CONSOLE 的 stdout 可被管道读取（Popen 同时支持）。验证 main 集成不破坏。

### PTC-09：终端版无回归 `[自动]`

- **测试内容**：`python main.py` 终端版仍正常
- **测试目的**：验证弹窗改动未污染 main.py（应为零改动）
- **测试过程**：
  1. 跑现有 `test_runner.py`（TC-01/02/03 等自动用例）
- **预期结果**：
  - 全部通过，与改前一致
- **自动化说明**：复用现有测试套件。弹窗方案对 main.py 零改动，此用例应为绿。

---

## 四、手动确认（物理交互 / 视觉 / 音频）

### PTC-10：物理按 Ctrl+Shift+A 弹出窗口 `[手动]`

- **测试内容**：人手按热键，控制台是否弹出
- **测试过程**：
  1. 启动 `python popup_trainer.py`（控制台隐藏）
  2. 物理按 `Ctrl+Shift+A`
- **预期**：控制台弹出并置顶。再按一次隐藏。

### PTC-11：弹出后聚焦可立即敲字 `[手动]`

- **测试内容**：弹出后输入光标是否在控制台，可立即敲字
- **测试过程**：弹出后不点鼠标，直接敲 `01-01` 选教材
- **预期**：直接生效，无需先点窗口

### PTC-12：颜色 / 按键 / 打断与终端版逐字一致 `[手动]`

- **测试内容**：弹窗控制台里的 UX 与 VS 终端版是否完全一致
- **测试过程**：在弹窗里走一遍模式 3（听写），观察绿/红色字、msvcrt 任意键打断 TTS
- **预期**：颜色一模一样；TTS 播放中按任意键能打断

### PTC-13：录音 / 回放 `[手动]`

- **测试内容**：弹窗模式下录音与回放正常
- **测试过程**：弹窗里走模式 1，录音回答，听回放
- **预期**：录音、回放正常，任意键可停止

### PTC-14：热键冲突提示 `[手动]`

- **测试内容**：主组合被其他软件占用时，是否正确提示失败
- **测试过程**：先开一个占用 Ctrl+Shift+A 的软件，再启动弹窗程序
- **预期**：日志出现 `[POPUP] hotkey failed`

---

## 汇总

| 类型 | 数量 | 用例 |
|---|---|---|
| `[自动]` | 9 | PTC-01, 02(易抖动), 03, 04, 05, 06, 07, 08, 09 |
| `[手动]` | 5 | PTC-10, 11, 12, 13, 14 |

**优先级**：PTC-01/02/03 是"热键是否有效"的核心三连，必须先过。
