# 多收藏夹设计

> 配套程序：`main.py`（改动），`favorites.json`（结构升级）
> 状态：v5.1 — 新增 `[U] 撤销`（防误操作）
> 设计目标：进"不熟"夹只遇到不熟词，练熟一个走一个（[P] 通过即移出），[U] 可撤销误操作。

---

## 1. 核心 UX 闭环

### 关键洞察

- **彻底删"全部"夹**——它是用户痛点根源（"他妈的得把所有的都看一遍"）
- **默认入口 = `[*1] 不熟` 夹**——只练真不熟的词
- **`[P]` 通过 = 自动从当前 set 移出 = 变熟**——用户主动按，不是系统判定
- **`[U]` 撤销 = 把最近一次 [P] 标熟的词加回 set**——防误操作
- **没有 `[Shift+F]`**、**没有系统判定**

### 完整工作流

```
[T] 选教材
  ├─ [*1] 不熟（8 词）            ← 默认入口（最不熟的第一级）
  ├─ [*2] 非常不熟（3 词）
  ├─ [1] 01-01                    ← 单课复习（新词发现场景）
  └─ [M] 收藏夹管理

[1] 01-11 模式 1 听写 → 遇到新词 "dormitorio" → [F]
  → by_lesson[01-11] += "dormitorio"
  → level_1.items += "dormitorio"
  → ★ 已收藏到「不熟」    ← 词现在在"不熟"夹里

[*1] 不熟夹练习 → 遇到 "dormitorio" → 答对 → [P] 通过
  → level_1.items 移除 "dormitorio"   ← 自动移出
  → push (_undo_stack, ("不熟", "dormitorio"))
  → ★ 已通过，自动从「不熟」移除（变熟）

（按错了）→ [U] 撤销
  → pop _undo_stack
  → level_1.items += "dormitorio"   ← 词回到"不熟"夹
  → ★ 撤销标熟：dormitorio 已回到「不熟」

[*1] 不熟夹练习 → 仍不熟 → [F]
  → 词复制到 level_2.items
  → ★ 已晋升到「非常不熟」
```

---

## 2. 数据结构

### 新格式

```json
{
  "by_lesson": {
    "01-11": ["dormitorio", "habitación", ...],
    "01-12": ["hermanos", ...]
  },
  "sets": [
    {"name": "不熟",       "order": 1, "items": ["dormitorio", "biblioteca"]},
    {"name": "非常不熟",   "order": 2, "items": ["te levantas"]},
    {"name": "极其不熟",   "order": 3, "items": []}
  ]
}
```

### 字段说明

| 字段 | 类型 | 含义 |
|------|------|------|
| `by_lesson` | dict | 词"出生地"（兼容字段） |
| `sets` | list | 不熟度夹，按 `order` 升序 |
| `sets[].name` | str | 收藏夹名（唯一） |
| `sets[].order` | int | 不熟度等级（1, 2, 3...；数值越大越不熟） |
| `sets[].items` | list | 词列表（同一词可在多 set，复制语义） |

### 关键约束

- **无 `order=0` virtual 夹**
- **无 `default_set` 字段**
- by_lesson **保留兼容**
- 词在 by_lesson 但不在任何 set = 隐式"熟"

### 默认初始化

启动时若无 `sets`：
```python
sets = [
    {"name": "不熟",     "order": 1, "items": []},
    {"name": "非常不熟", "order": 2, "items": []},
]
```

---

## 3. UI 设计

### 3.1 教材选单 (`select_textbook`)

```
====================================
          西班牙语陪练
====================================
  请选择教材：

  [*1] 不熟（8 词）               ← 默认入口
  [*2] 非常不熟（3 词）
  [*3] 极其不熟（0 词）
  [1] 01-01（16个生词 / 0条例句 / 1个语法点）
  [2] 01-02（...）
  ...
  [M] 收藏夹管理
  [Q] 退出
====================================

请选择（教材代码；*1-*9=收藏夹；M=管理；Q=退出）>
```

**没有 `[*]` 全部入口**——彻底删除。

### 3.2 主菜单

```
[0] 记忆导入
[1] 听西语说中文
[2] 听中文说西语
[3] 听写
[4] 跟读
[5] 混着来
[G] 语法讲解
[M] 收藏夹管理
[T] 选教材
[Q] 退出
```

### 3.3 `[M]` 收藏夹管理

```
====================================
          [收藏夹管理]
====================================
  [*1] 不熟 (8 词)
  [*2] 非常不熟 (3 词)
  [*3] 极其不熟 (0 词)

  [N] 新建收藏夹
  [R] 重命名选中
  [D] 删除选中
  [O] 调顺序（上移/下移）
  [V] 查看内容
  [Q] 返回主菜单
====================================
```

### 3.4 各模式入口

```
====================================
          [模式1-单词] 共 3 组
====================================
  [R] 随机组（16 个词，乱序）
  [*1] 不熟（8 词）
  [*2] 非常不熟（3 词）
  [1] 第 1 组：novio, ...
  [2] 第 2 组：tío, ...
  [3] 第 3 组：...
  [M] 收藏夹管理
  [B] 返回
====================================
```

### 3.5 练习中按键菜单（**v5.1 关键差异**）

#### 不熟夹练习（**新增 [U]**）

```
  [Enter/P]通过  [N]保留  [B]上词  [R]重听  [G]下组  [F]收藏  [U]撤销  [Q]退出 > _
```

#### 普通组/单课练习（**无 [U]**）

```
  [Enter/P]通过  [N]保留  [B]上词  [R]重听  [G]下组  [F]收藏  [Q]退出 > _
```

`[U]` **只**在不熟夹练习中显示，避免普通组误用。

---

## 4. `[F]` `[P]` `[U]` 行为（核心）—— 用人话讲

### 4.0 先搞清楚"在哪儿按"——这决定了行为不一样

你按这三个键时，**所在的练习不一样**，结果也不一样：

| 你在哪儿按 | 练的是什么词 | 例子 |
|-----------|------------|------|
| **在不熟夹里练**（选了 `[*1] 不熟` / `[*2] 非常不熟`...） | 那个不熟夹里的词 | 在"不熟"夹里练 "dormitorio" |
| **在单课/普通组里练**（选了 `[1] 01-11` 某课，或 `[1] 第 1 组`） | 那一课的词 | 在"01-11 第 1 组"里练 "novio" |

**核心区别：不熟夹里 [P] 有"标熟"魔法，单课里没有。** 不熟夹的词会"消失"，单课的词不会。

---

### 4.1 `[F]` ——"这词不熟，加到不熟夹里"

不管你在哪儿按，逻辑都一样：**让这个词进入"不熟"链**。

| 你按 [F] 时这个词在哪儿 | 发生什么 | 屏幕反馈 |
|----------------------|---------|---------|
| 完全没见过这词 | 记到当前课的 by_lesson + 加进「不熟」夹 | `★ 已收藏到「不熟」` |
| 这词在 by_lesson，但**不在**任何不熟夹（= 隐式"熟"） | 加进「不熟」夹（重新标不熟） | `★ 已收藏到「不熟」` |
| 这词已经在「不熟」夹（不熟夹 1） | **复制**一份到「非常不熟」夹（加重） | `★ 已晋升到「非常不熟」` |
| 这词已经在「非常不熟」夹（最高级） | 啥也不做（已经最不熟了） | `★ 已在「非常不熟」（最高级），无变化` |

简单说：`[F]` 一直按"加重不熟"的方向走，走到顶就停。

---

### 4.2 `[P]` ——"通过了"——**这是关键，搞混了就要命**

**`[P]` 在不熟夹 vs 单课，行为完全不一样**：

#### 场景：你选 `[*1] 不熟` 进了不熟夹，遇到词 "dormitorio"，打对了按 [P]

```
★ 已通过，自动从「不熟」移除（变熟）
```

**发生了什么**：
- "dormitorio" 从「不熟」夹里**消失了**——你以后选 `[*1] 不熟` 不会再看到它
- 它"变熟"了（隐式，不再进任何不熟夹的练习）

#### 场景：你在 `01-11 第 1 组` 单课里练，遇到 "novio"，打对了按 [P]

（main 现有的 GroupSession 行为——这个词从这组里通过，下次这组不会重复出现它。仅此而已。**这个词不会从任何不熟夹里消失**。）

**对比**：
- **不熟夹**：[P] = **标熟 + 词消失**（魔法效果）
- **单课**：[P] = **通过 + 这组里过**（普通效果，不动收藏夹）

**为什么这么设计**：你进不熟夹就是为了"练熟一个走一个"；你在单课是为了复习整课，不会想动收藏夹。

#### 详细规则表

| 你在哪儿按 [P] | 词在哪儿 | 发生什么 |
|---------------|---------|---------|
| **不熟夹** `[*1] 不熟` | 词在「不熟」夹 | **从「不熟」移除**（标熟）+ 记录"可撤销" | `★ 已通过，自动从「不熟」移除（变熟）` |
| **不熟夹** `[*2] 非常不熟` | 词在「非常不熟」夹 | **从「非常不熟」移除** + 记录"可撤销" | `★ 已通过，自动从「非常不熟」移除（变熟）` |
| **不熟夹** `[*1] 不熟` | 词同时在「不熟」和「非常不熟」 | **只从「不熟」移除**（非常不熟还在） | `★ 已通过，自动从「不熟」移除（变熟）` |
| **单课** `[1] 01-11` | 词在「不熟」夹 | **啥也不动收藏夹**（只算这一组通过） | main 现有反馈 |

**一个关键点**：词同时在多个不熟夹时，[P] 只从**你正在练的**那个夹移除。

---

### 4.3 `[U]` ——"我刚才按错了，撤销最近一次标熟"

`[U]` **只**在不熟夹练习里出现，单课里没有这个键（按了也没反应）。

**它干什么**：你刚才在不熟夹里按 [P] 把一个词标熟了——如果发现按错了（其实该按 [N]），按 [U] 把词**放回**原来的不熟夹。

#### 例子

```
你在「不熟」夹练 biblioteca
  > bilioteca        ← 拼错
  ✗ 不对，再试试？
  > biblioteca       ← 对了
  ✓ 好的

  [P] > p   ← 哎呀其实我是想按 [N] 保留的啊！

  ★ 已通过，自动从「不熟」移除（变熟）    ← 词消失了

  [U] > u   ← 撤销！

  ★ 撤销标熟：biblioteca 已回到「不熟」    ← 词回来了
```

#### 规则表

| 状态 | [U] 干了啥 | 反馈 |
|------|----------|------|
| 你在不熟夹刚标熟过词（"可撤销栈"里有东西） | 把那个词**放回**原来的不熟夹 | `★ 撤销标熟：<词> 已回到「<夹名>」` |
| "可撤销栈"是空的（没标熟过任何词，或已经撤完了） | 啥也不做 | `★ 无可撤销` |
| 你在**单课**练习里按 [U] | 没反应（这个键在单课里不显示） | （没反应） |

**几个细节**：
- 最多保留**最近 5 次**标熟——可以连按 [U] 撤销 5 步，再多就没了
- **切换不熟夹**（比如从「不熟」切到「非常不熟」）时，撤销栈**清空**——不然"撤销"会跨夹搞混
- 撤销不影响"主菜单"和"不熟夹练习"的 UI——词就静静回来，列表刷新一下就看到

---

### 4.4 一张表总结三个键

| 键 | 在不熟夹里 | 在单课里 |
|----|----------|---------|
| `[F]` | 加重不熟（自动，可能晋升到下一级） | 加重不熟（自动，可能晋升到下一级） |
| `[P]` | **标熟 = 词从这个不熟夹消失**（可撤销） | 普通通过（不动收藏夹） |
| `[U]` | 撤销上一次 [P] 标熟（把词放回） | 不会出现 / 没反应 |

记住一句话：**不熟夹里 [P] 是魔法键，按了词就消失；单课里 [P] 就是普通通过。**

---

### 4.5 完整流程例子

#### 例子 1：在某课遇到不熟新词 → [F] 加进不熟夹

```
[T] 选教材 → [1] 01-11

  dormitorio        ← 听写模式遇到这个词
  > dormitorio
  ✓ 拼写正确！

  [F] > f

  ★ 已收藏到「不熟」    ← 词现在在「不熟」夹里
```

#### 例子 2：进"不熟"夹练 → [P] 通过 = 词消失

```
[T] 选教材 → [*1] 不熟（8 词）

  dormitorio        ← 「不熟」夹里出现这个词
  > dormitorio
  ✓ 拼写正确！

  [P] > p

  ★ 已通过，自动从「不熟」移除（变熟）    ← 「不熟」夹变成 7 词
```

#### 例子 3：误按 [P] → [U] 撤销

```
  biblioteca
  > bilioteca
  ✗ 不对，再试试？
  > biblioteca
  ✓ 好的

  [P] > p   ← 哎呀该按 [N] 的！

  ★ 已通过，自动从「不熟」移除（变熟）

  [U] > u   ← 撤销！

  ★ 撤销标熟：biblioteca 已回到「不熟」    ← 词回来了，「不熟」夹变回 8 词
```

#### 例子 4：[F] 加重不熟（从「不熟」升到「非常不熟」）

```
  biblioteca        ← 在「不熟」夹里练，仍不熟
  > bilioteca
  ✗ 不对，再试试？
  > biblioteca
  ✓ 好的

  [F] > f

  ★ 已晋升到「非常不熟」    ← 词同时在「不熟」和「非常不熟」里
```

#### 例子 5：[F] 加重不熟（已经是最高级了）

```
  biblioteca        ← 在「非常不熟」夹里练（已经是最高级）
  > bilioteca
  ✗ 不对，再试试？
  > biblioteca
  ✓ 好的

  [F] > f

  ★ 已在「非常不熟」（最高级），无变化    ← 啥也不做
```

#### 例子 6：[U] 撤销栈空了

```
  连续通过 5 个词，撤销栈已满又清空

  [U] > u

  ★ 无可撤销    ← 没东西可撤了
```

#### 例子 7：在单课里 [F] 加进不熟夹

```
[T] 选教材 → [1] 01-11

  biblioteca        ← 01-11 里遇到这个词
  > biblioteca
  ✓ 拼写正确！

  [F] > f

  ★ 已收藏到「不熟」    ← 词从 01-11 视角被标"不熟"
                          之后选 [*1] 不熟 就能练到
```

#### 例子 8：单课里 [P] = 普通通过（**不动收藏夹**）

```
[T] 选教材 → [1] 01-11 → [1] 第 1 组

  novio        ← 第 1 组里这个词
  > novio
  ✓ 拼写正确！

  [P] > p

  ✓ ✓                                ← main 现有反馈
  下一题 →

  （「不熟」夹里没有"novio"——单课 [P] 不会动收藏夹）
```

#### 例子 9：词在多个不熟夹，[P] 只移出当前夹

```
  「不熟」夹里有 biblioteca
  「非常不熟」夹里也有 biblioteca
  你现在在「不熟」夹练它，[P] 通过

  ★ 已通过，自动从「不熟」移除（变熟）

  （「非常不熟」夹里 biblioteca 还在）
  之后选 [*2] 非常不熟 还能练到它
```

---

## 5. 关键设计决策（v3 → v4 → v5 → v5.1）

| 决策点 | v3 | v4 | v5 | **v5.1** |
|--------|----|----|----|--------|
| 默认入口 | `[*] 全部` (by_lesson) | `[*] 全部` (不熟并集) | **`[*1] 不熟` 夹** | 同 v5 |
| "全部"夹 | by_lesson 并集 | 不熟并集 | **彻底删除** | 同 v5 |
| 标熟动作 | 无 | `[Shift+F]` 手动 | **`[P]` 通过 = 自动** | 同 v5 |
| **撤销** | 无 | 无 | 无 | **`[U]` 撤销** |
| 系统判定 | 无 | 无 | 无 | 无 |
| [F] 加重不熟 | ✓ | ✓ | ✓ | ✓ |
| 复制语义 | ✓ | ✓ | ✓ | ✓ |
| order=0 virtual | 有 | 无 | 无 | 无 |
| default_set | 有 | 移除 | 移除 | 移除 |

---

## 6. 代码改动

### 6.1 `main.py` 改动

| 函数 | 行 | 改动 |
|------|---|------|
| `_load_favorites()` | `main.py:913-923` | 加载 sets（无 order=0） |
| `_save_favorites(fav)` | `main.py:925-929` | 保存（by_lesson + sets） |
| `_build_favorites_textbook(name)` | `main.py:284-304` | name 必是非 virtual set 名 |
| `_get_favorites()` | `main.py:932-937` | 返回指定 set items |
| `_toggle_favorite(es_text)` | `main.py:940-967` | `[F]` 全自动（v3 规则） |
| `_remove_from_current_set(es_text)` | - | `[P]` 在 set 练习中触发 |
| `_undo_last_mark()` | - | **`[U]` 撤销**（v5.1 新增） |
| `select_textbook()` | `main.py:315-358` | 删"全部"入口 |
| `print_menu()` | `main.py:1104-1119` | 加 `[M]` |
| `_run_group_menu_*` | `main.py:1154/1354/1819/2090` | 不熟夹子菜单 |
| `_run_one_group_*` | `main.py:1198/1403/1857/2090` | 追踪 `_current_set_name` |
| `_decision_pnbr` / 各 P 处理 | `main.py:1714+` | 在 set 练习中 P 通过 → `_remove_from_current_set` + push `_undo_stack` |
| 各模式按键菜单 | `main.py:1227/1660/2534/2576/2767/2830` | **不熟夹中加 `[U]撤销` 选项** |
| `main()` | `main.py:3014-3053` | 加 `[M]` 分支 |
| **新增** `mode_m_favorites()` | - | 收藏夹管理 |
| **新增** `_current_set_name` 全局 | - | 追踪当前 set |
| **新增** `_undo_stack` 全局 | - | 撤销栈 |
| **新增** `_set_current_set_name(name)` | - | 进入 set 练习时设 |
| **新增** `_clear_current_set_name()` | - | 退出 set 练习时清（含清 _undo_stack） |

### 6.2 `_undo_stack` 与 `_current_set_name` 机制

```python
# 全局状态
_undo_stack: list[tuple[str, str]] = []  # [(set_name, es_word)]
_UNDO_MAX = 5
_current_set_name: str | None = None     # 当前练习的 set 名

def _set_current_set_name(name):
    global _current_set_name, _undo_stack
    _current_set_name = name
    _undo_stack = []  # 切换 set 时清空撤销栈

def _clear_current_set_name():
    global _current_set_name, _undo_stack
    _current_set_name = None
    _undo_stack = []

def _remove_from_current_set(es_text):
    """[P] 在 set 练习中通过时调用：从当前 set.items 移出词"""
    global _undo_stack
    if _current_set_name is None:
        return
    for s in _load_favorites()["sets"]:
        if s["name"] == _current_set_name and es_text in s["items"]:
            s["items"].remove(es_text)
            _save_favorites(_load_favorites())
            _undo_stack.append((_current_set_name, es_text))
            if len(_undo_stack) > _UNDO_MAX:
                _undo_stack.pop(0)
            return

def _undo_last_mark():
    """[U] 触发：把最近一次标熟撤销（词加回 set）"""
    global _undo_stack
    if not _undo_stack:
        print("  ★ 无可撤销")
        return
    set_name, es_word = _undo_stack.pop()
    fav = _load_favorites()
    for s in fav["sets"]:
        if s["name"] == set_name:
            if es_word not in s["items"]:
                s["items"].append(es_word)
            _save_favorites(fav)
            print(f"  ★ 撤销标熟：{es_word} 已回到「{set_name}」")
            return
```

### 6.3 `favorites.json` 改动

启动检测：
- 无 `sets` → 初始化 v5 结构
- 有 v3 格式（order=0 virtual）→ 自动转 v5
- by_lesson 完整保留

### 6.4 改动量

~450-550 行，main.py。

### 6.5 不改

- `popup_trainer.py`
- `教材/*.txt`
- `spanish_learner.py`
- 现有 `test_*.py`

---

## 7. 测试

### 7.1 新增用例

| ID | 用例 | 状态 |
|----|------|------|
| MS-01 | 老 favorites.json 自动迁移 | 自动 |
| MS-02 | v3 格式（order=0）转 v5 | 自动 |
| MS-03 | 教材选单**无** `[*]` 全部入口 | 自动 |
| MS-04 | 教材选单显 `[*1]` `[*2]` | 自动 |
| MS-05 | `[F]` NOT_IN_FAV → by_lesson + level_1 | 自动 |
| MS-06 | `[F]` IN_FAV_ONLY → level_1 | 自动 |
| MS-07 | `[F]` 加重不熟 = 复制到下一级 | 自动 |
| MS-08 | `[F]` 最高级 → 反馈无变化 | 自动 |
| MS-09 | `[P]` 在 level_1 练习通过 → 从 level_1.items 移除 | 自动 |
| MS-10 | `[P]` 在 level_2 练习通过 → 从 level_2.items 移除 | 自动 |
| MS-11 | `[P]` 在某课普通组通过 → 不动 set | 自动 |
| MS-12 | 复制语义：词在多 set → `[P]` 只移当前 set | 自动 |
| **MS-13** | **`[U]` 在不熟夹 → 撤销最近 [P]，词回 set** | 自动 |
| **MS-14** | **`[U]` 无可撤销 → 提示"无可撤销"** | 自动 |
| **MS-15** | **`[U]` 切换 set 后栈清空** | 自动 |
| **MS-16** | **普通组不显示 [U] 键** | 自动 |
| MS-17 | `[M]` 各管理操作 | 手动 |
| MS-18 | 终端版回归 | 自动 |

### 7.2 回归

跑 `test_runner.py` 12 条。关注涉及 [F] 行为的 TC-09/16/17/18/20。

---

## 8. 风险

| 风险 | 缓解 |
|------|------|
| 删"全部"夹 — 用户预期 | 文档说明 |
| `[P]` 在 set 中移出 — 普通组 vs set 行为不同 | UI 明确分组（不熟夹/单课组） |
| `_current_set_name` 全局状态 | 进入/退出 set 练习时严格 set/clear |
| `_undo_stack` 跨 set 累计 | 切换 set 时清空 |
| `[U]` 撤销后状态不一致 | 词加回 set.items + 立即 save |
| 复制语义 + `[P]` 移出 | 明确文档：词在多 set 时只移当前 set |
| 老 v3 格式 | 启动自动转 v5 |
| popup_trainer 兼容 | popup import main，函数签名不变 |

---

## 9. 未来扩展（v6+）

- `[Ctrl+Shift+F]` 批量标熟（多选）
- 答对 N 次自动标熟（v5 明确不要，但 v2 评估时可重提）
- 收藏夹图标/emoji
- 收藏夹导入/导出
- 错题统计联动

---

## 10. 验收标准

- [ ] 教材选单**无** `[*]` 全部入口
- [ ] 教材选单显 `[*1]` `[*2]` 不熟夹入口
- [ ] 进"不熟"夹只遇到不熟词
- [ ] `[P]` 在不熟夹通过 = 词自动从该夹移除 + 反馈
- [ ] `[P]` 在单课普通组通过 = 不动 set
- [ ] **`[U]` 在不熟夹 = 撤销最近 [P] 标熟，词回 set**
- [ ] **`[U]` 在普通组不显示 / 无效**
- [ ] **`[U]` 无可撤销时提示"无可撤销"**
- [ ] **`[U]` 切换 set 后栈清空**
- [ ] `[F]` NOT_IN_FAV → by_lesson + level_1
- [ ] `[F]` IN_FAV_ONLY → level_1
- [ ] `[F]` 加重不熟 = 复制到下一级
- [ ] 复制语义：词在多 set 时 `[P]` 只移当前 set
- [ ] `[M]` 管理 N/R/D/O/V 全可用
- [ ] by_lesson 字段完整保留兼容
- [ ] 终端版 test_runner.py 12 条测试无新失败
- [ ] 用户**无需** `[Shift+F]` 整理，**无需**系统判定，**误操作可撤销**
