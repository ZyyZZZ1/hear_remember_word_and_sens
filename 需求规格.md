# 西班牙语陪练程序 —— 需求规格

---

## 1. 概述

一个终端运行的西班牙语陪练程序。教材内容内置在代码中，用户打开即用。程序的核心行为是：朗读西语内容 → 录音用户的回答 → 回放用户的录音和正确答案 → 用户自判对错 → 答错的在本轮内多练。不联网，不存储学习记录，不做复习计划。

---

## 2. 数据

### 2.1 教材数据

教材数据以 Python 数据结构直接写在代码中，包含以下内容：

- **生词列表**：每条包含西语单词、中文释义
- **例句列表**：每条包含西语句子、中文翻译、所属语法点
- **语法点列表**：每条包含语法点名称、中文说明、关联的例句

数据示例：

```
生词：novio（男朋友）、lengua（语言）、universidad（大学）、país（国家）……
例句：Ricardo es el novio de Rosa.（里卡多是罗莎的男朋友。）……
语法点：介词 DE 的所属用法、介词 EN 的地点用法……
```

### 2.2 无持久化

程序退出后不保留任何练习记录。每次启动都是全新状态。

---

## 3. 功能模式

### 3.1 主菜单

程序启动后显示主菜单，列出可用模式和对应按键：

| 按键 | 模式 |
|---|---|
| 1 | 听西语说中文 |
| 2 | 听中文说西语 |
| 3 | 听写 |
| 4 | 跟读 |
| 5 | 混着来 |
| G | 语法讲解 |
| Q | 退出程序 |

### 3.2 模式 1：听西语说中文

**适用内容**：生词、例句。

**流程**：

1. 程序用 TTS 朗读一个西语词或句子
2. 播放提示音，开始录音
3. 用户说出对应的中文意思
4. 用户按 Enter 结束录音（或程序检测到静音后自动结束）
5. 程序依次播放：用户的录音 → 正确答案（TTS 朗读西语原词/原句，屏幕显示中文释义）
6. 用户按键判断：
   - Y：答对了
   - N：答错了（该词回到队尾）
7. 进入下一题

**中间可用的按键**：

| 按键 | 功能 |
|---|---|
| R | 重听一遍原词发音 |
| S | 跳过，该词回到队尾，不计对错 |
| D | 翻转方向，切换到模式 2 |
| Q | 退出回主菜单 |

**通过条件**：同一个词连续两次得到 Y 后，本轮不再出现。

### 3.3 模式 2：听中文说西语

与模式 1 方向相反。流程相同：

1. 程序用 TTS 朗读中文意思（注：如果不能 TTS 中文，则在屏幕上显示中文，用提示音代替朗读）
2. 录音用户说的西语
3. 回放用户录音 → 播放正确答案（TTS 朗读西语）
4. 用户 Y/N 自判

其余按键和通过条件同模式 1。

### 3.4 模式 3：听写

**适用内容**：生词。

**流程**：

1. 程序用 TTS 朗读一个西语单词
2. 用户在键盘上输入拼写，按 Enter 提交
3. 程序比对拼写：
   - 完全正确：提示"对"，进入下一题
   - 拼写错误：显示正确拼写，TTS 逐个字母朗读正确拼写，该词回到队尾
4. 进入下一题

**中间可用的按键**：

| 按键 | 功能 |
|---|---|
| R | 重听一遍单词发音 |
| S | 跳过，该词回到队尾 |
| Q | 退出回主菜单 |

**通过条件**：同一个词连续两次拼写正确后，本轮不再出现。

**注意**：这是唯一由程序自动判罚的模式——键盘拼写比对是确定性的，不依赖语音识别。

### 3.5 模式 4：跟读

**适用内容**：例句。

**流程**：

1. 程序用 TTS 朗读一句西语
2. 播放提示音，开始录音
3. 用户跟读
4. 用户按 Enter 结束录音（或静音自动结束）
5. 程序交替播放：先播原句 → 再播用户的录音
6. 用户按键判断：
   - Y：跟读满意
   - N：不满意
   - R：再听一遍对比（重复步骤 5）
7. 如果按 N，可以立即再跟读同一句；如果按 Y，进入下一句

**中间可用的按键**：

| 按键 | 功能 |
|---|---|
| R | 再听一遍原句 + 录音对比 |
| S | 跳过，进入下一句 |
| Q | 退出回主菜单 |

**通过条件**：同一句连续两次 Y 后，本轮不再出现。

**注意**：此模式完全不依赖语音识别。程序只做录音和播放。

### 3.6 模式 5：混着来

随机混合模式 1、2、3 的题型。每道题按对应模式的规则执行。听写题由程序自动判罚，其余题由用户 Y/N 自判。所有按键（Y/N/S/R/D）均有效。D 键在非听写题中切换方向。

### 3.7 语法讲解（G 键）

1. 屏幕列出所有语法点
2. 用户按数字键选择一个语法点
3. 屏幕显示该语法点的中文说明
4. TTS 逐条朗读该语法点关联的西语例句
5. 按 R 重听当前例句，按 Q 返回语法点列表，再按 Q 回主菜单

---

## 4. 错误循环规则

适用于模式 1、2、3、4、5，规则统一：

- 用户判定为"错"（N 或拼写错误）→ 该词/句排到队尾，稍后再次出现
- 用户判定为"对"（Y 或拼写正确）一次 → 该词/句排到队尾，再出现一次以巩固
- 连续两次"对" → 该词/句本轮不再出现
- 用户跳过（S）→ 排到队尾，不计对错
- 所有队列和计数在程序退出时丢弃

---

## 5. 音频功能

### 5.1 TTS 朗读

- 使用系统自带 TTS 引擎朗读西语（Windows SAPI5 西语语音）
- 单词朗读：常速读一遍
- 句子朗读：常速读一遍，跟读模式下可稍慢
- 中文内容不强制朗读，优先屏幕显示；若系统有中文语音包则朗读

### 5.2 录音

- 通过麦克风录制用户语音
- 触发方式：提示音后自动开始录制
- 结束方式：用户按 Enter，或检测到持续静音后自动结束
- 录音保存为临时音频，播放后丢弃

### 5.3 录音回放

- 每次用户回答后，播放用户录音
- 随后播放正确答案（TTS），形成"你的录音 → 正确答案"的对比
- 跟读模式下，播放"原句 → 你的录音 → 原句 → 你的录音"的交替对比

---

## 6. Whisper 集成（可选功能）

### 6.1 定位

Whisper 不作为判罚依据，仅作为可选的视觉辅助。

### 6.2 功能

在模式 1 和模式 2 中，如果用户开启了"字幕"选项：

- 用户录音结束后，Whisper 将录音转写为文字
- 转写结果显示在屏幕上，供用户参考

### 6.3 开关

- 默认关闭
- 在主菜单或练习中可通过按键开关
- 关闭后不影响任何核心功能
- 未安装 Whisper 时程序正常运行

---

## 7. 按键总览

| 按键 | 功能 | 适用场景 |
|---|---|---|
| 1 | 听西语说中文 | 主菜单 |
| 2 | 听中文说西语 | 主菜单 |
| 3 | 听写 | 主菜单 |
| 4 | 跟读 | 主菜单 |
| 5 | 混着来 | 主菜单 |
| G | 语法讲解 | 主菜单 |
| Q | 退出程序 / 返回上级 | 全局 |
| Y | 判定"答对了" | 练习中（模式 1/2/4/5） |
| N | 判定"答错了" | 练习中（模式 1/2/4/5） |
| S | 跳过当前题 | 练习中 |
| R | 重听 / 重新对比 | 练习中 |
| D | 翻转答题方向 | 模式 1/2/5 |
| Enter | 结束录音 / 提交拼写 | 练习中 |

---

## 8. 技术约束

- 运行环境：Windows，Python 3
- TTS：pyttsx3 + 系统 SAPI5 西语语音
- 录音：sounddevice 或 pyaudio
- 语音识别（可选）：faster-whisper small 模型，本地离线运行
- 不依赖：任何云 API、数据库、LLM、用户账号系统
