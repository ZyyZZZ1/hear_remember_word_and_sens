# 热键调出答题窗口 — 设计方案

## 目标

单文件 `popup_trainer.py`，`python` 启动后控制台隐藏常驻；
按 `Ctrl+Shift+A` 唤出控制台并聚焦，在里面敲字答题；
UX、颜色、按键、打断与 `main.py` 终端版逐字一致，`main.py` 零改动。

---

## 方案：真控制台 + 全局热键

用真控制台（而非 tkinter 伪终端），因为：

- **颜色一模一样**：main 的 ANSI 码由控制台原生解析，不是翻译复刻。
- **按键一模一样**：真控制台，没有 GUI 控件的默认绑定干扰。
- **msvcrt 打断原生有效**：main 的 `kbhit()/getch()`（任意键打断 TTS/录音）在真控制台里原生工作，无需适配。
- **main.py 零改动**：直接 `import main; main.main()`，与 `python main.py` 相同入口。
- **它就是 terminal**：外观无需调校即像终端。

---

## 架构（单进程单文件）

```
popup_trainer.py
 ├─ 启动时：设控制台标题/尺寸/居中 → 注册热键 → 隐藏控制台
 ├─ 主线程：import main; main.main()       # 阻塞跑，与终端版相同
 └─ 后台线程：RegisterHotKey(Ctrl+Shift+A)
       └─ WM_HOTKEY → GetConsoleWindow() → ShowWindow / SetForegroundWindow
```

- 单进程，控制台就是本进程的控制台，`GetConsoleWindow()` 一行拿句柄。
- `ShowWindow` 是线程安全 API，后台线程调用不影响主线程跑 `main.main()`。

---

## 全局热键（无管理员）

后台线程：

1. `win32gui` 创建消息专用隐藏窗口（`HWND_MESSAGE`，不可见、不在任务栏）。
2. `user32.RegisterHotKey(hwnd, id=1, MOD_CONTROL|MOD_SHIFT, vk=VK_A)`。`VK_A` = 字母 A，不依赖键盘布局。
3. `win32gui.PumpMessages()` 收 `WM_HOTKEY`(0x0312) → 切换显隐。

要点：
- 不用 `keyboard`/`pynput`（低级钩子常要管理员权限）；`RegisterHotKey` 普通权限即可。
- RegisterHotKey 不区分左右 Ctrl/Shift，但三键齐按才触发，单键无干扰。
- 注册失败（组合被占用）→ 日志提示 `hotkey failed`。

---

## 控制台显隐与聚焦

热键触发（后台线程直接调 Win32 API）：

```
hwnd = kernel32.GetConsoleWindow()

隐藏 → 唤出：
   ShowWindow(hwnd, SW_RESTORE)          # 恢复
   SetWindowPos(hwnd, HWND_TOPMOST, ...) # 置顶
   SetForegroundWindow(hwnd)             # 激活 → 光标天然聚焦

显示 → 隐藏：
   ShowWindow(hwnd, SW_HIDE)             # 彻底隐藏，任务栏不残留
```

- 控制台被激活后输入光标天然在窗口内，等价"自动聚焦输入框"，比 GUI 的 `focus_set` 更可靠。
- 收起只靠热键（不在控制台里全局拦 Esc，避免干扰 main 的输入）。
- 启动后立即 `SW_HIDE`，再跑 `main.main()`。控制台隐藏不影响 main 运行——它阻塞在 `stdin.readline()`，用户按热键唤起后即可看到菜单并输入。

---

## 控制台外观（启动时设置一次）

- **标题**：`SetConsoleTitleW("SpanishDrill")`（或伪装成无害名）。
- **尺寸**：约 38 列 × 9 行（七八行），`MoveWindow` 缩小窗口。控制台最小尺寸受字体限制，无法无边框做到 3cm，但 38×9 已紧凑。
- **字体/颜色**：`SetCurrentConsoleFontEx` 设 Consolas 小号；黑底白字由 main 的 ANSI 原生控制。
- **位置**：居中。
- **取舍**：控制台有标题栏和边框，不能无边框。这是"像 terminal + 交互一致 + 零改动"的必要代价。

---

## 启动流程

```
python popup_trainer.py
  → 控制台出现（几十毫秒）
  → 设标题 / 尺寸 / 居中
  → 后台线程注册热键
  → 立即隐藏控制台（SW_HIDE）
  → 主线程 main.main() 开始跑（教材选择菜单已就绪，窗口隐藏用户看不见）
  → 用户按 Ctrl+Shift+A
  → 控制台显示 + 置顶 + 聚焦 → 看到菜单，直接敲字
  → 再按热键 → 隐藏（进度保留在 main 内存，不丢）
```

- 进入路径与 VS 终端 `python main.py` 完全相同，足够简单。
- 可选 `popup.bat`：`start "" python popup_trainer.py` 一键启动。

---

## 录音与打断

- `sounddevice` 录音 + `sd.play` 回放不依赖控制台可见性，正常工作。
- 录音/TTS 期间"任意键停止"走 `msvcrt`，真控制台下原生有效，无需适配。

---

## 依赖与改动

- **依赖**：无新增。pywin32 已装（`win32com` 在用，`win32gui`/`win32con` 同包）；`kernel32`/`user32` 走 ctypes（标准库）。
- **main.py**：零改动。`python main.py` 终端版照常，`popup_trainer.py` 弹窗版两路行为完全相同。

---

## 实施步骤

1. 环境确认：`python -c "import win32gui, win32con, ctypes"`。
2. 写 `popup_trainer.py`：后台热键线程 + `GetConsoleWindow` 显隐 + `main.main()`。
3. 调控制台标题/尺寸/居中。
4. 接入启动即隐藏 + 热键唤起。
5. 自测：
   - 热键弹出/隐藏、激活后光标聚焦可立即敲字。
   - 教材选择、主菜单、各模式操作，UX/颜色/按键与 VS 终端版逐字一致。
   - TTS 朗读正常、按任意键可打断。
   - 录音/回放正常。
   - `python main.py` 终端版无回归。

---

## 风险与取舍

1. **控制台最小尺寸**：不能无边框 3cm，38×9 字符是较紧凑极限。
2. **启动闪现**：`python` 启动闪一下控制台再隐藏（几十毫秒）；可接受或用 subprocess 隐藏启动。
3. **焦点窃取**：`SetForegroundWindow` 偶被系统抑制；本进程注册了热键，系统对热键来源进程放宽限制，通常无碍；必要时辅以 `AttachThreadInput`。
4. **热键占用**：Ctrl+Shift+A 被占用 → 注册失败，日志提示 `hotkey failed`。
5. **多实例**：重复启动多个控制台；可加互斥体单实例锁。
6. **控制台主机差异**：Windows Terminal 下 `GetConsoleWindow` 可能返回 0；实施时检测，必要时强制 conhost。
