# 教材管理 —— 用户交互设计

---

## 核心原则

- 每本教材是一个 `.txt` 文件，放在 `教材/` 目录下
- 文件名即教材编号（如 `01-08.txt`、`01-09.txt`）
- 不同教材之间互不相关，选哪本练哪本
- 语法点可选——有的教材有，有的没有

---

## 第一步：准备教材文件

用户在 `教材/` 目录下创建 `.txt` 文件，文件名自定（如 `01-09.txt`）。

文件内容分三个区段，每个区段以 `#` 开头的标记行分隔：

```
# 生词
西语1 中文1
西语2 中文2

# 例句
西语句子1 中文翻译1
西语句子2 中文翻译2

# 语法点
语法点名称1
语法点说明文字1
关联的例句序号，逗号分隔
```

**规则：**
- `# 生词`、`# 例句`、`# 语法点` 三个标记行各占一行
- 生词格式：`西语单词 中文释义`（空格分隔，一行一个）
- 例句格式：`西语句子 中文翻译`（用 `#` 或 `→` 或 tab 分隔，一行一对）
- 语法点：如果教材没有语法点，**整个 `# 语法点` 区段可以省略不写**，或者写了但下面没有任何内容
- 区段顺序无所谓（先生词后例句、先例句后生词都行）

**示例（01-09，只有生词和例句，无语法点）：**

```
# 生词
el comedor 餐厅
el dormitorio 卧室
la cocina 厨房

# 例句
En mi casa hay un comedor. 在我家有一个餐厅。
Mi dormitorio es muy grande. 我的卧室很大。
```

---

## 第二步：启动程序，选择教材

程序启动后，不再直接显示练习菜单。而是先扫描 `教材/` 目录，列出所有可用教材：

```
====================================
        西班牙语陪练
====================================

  请选择教材：

  [1] 01-08（12个生词 / 7条例句 / 2个语法点）
  [2] 01-09（15个生词 / 10条例句 / 无语法点）

  [Q] 退出
====================================
```

用户按数字键选择教材。选完后，程序加载该教材的全部内容，进入练习主菜单。

**如果 `教材/` 目录下只有一个文件，跳过选择步骤，直接加载。**

**如果 `教材/` 目录为空，程序提示"教材目录为空，请先放入教材文件"，然后退出。**

---

## 第三步：练习（不变）

选完教材后，进入原有的练习主菜单，所有模式的行为完全不变。只是数据来源切换为用户选择的教材。

教材之间完全隔离——选了 01-08 就只在 01-08 的词和句子里练，选了 01-09 就只在 01-09 里练。切换教材需要退出重进。

---

## 第四步：语法点缺失时的处理

如果用户选的教材没有语法点（即文件中没有 `# 语法点` 区段或区段为空）：

- 主菜单中 **[G] 语法讲解** 仍显示，但用户按 G 后，屏幕显示：

  ```
  ========================================
              语法讲解
  ========================================
    本教材没有语法点。
    [Q] 返回主菜单
  ========================================
  ```

- 其他模式不受影响

---

## 教材文件完整示例（01-08 迁移后的格式）

```
# 生词
Ricardo 里卡多（人名）
Rosa 罗莎（人名）
nuestra 我们的
lengua 语言
universidad 大学
este 这个
país 国家
del ……的（de+el缩写）
González 冈萨雷斯（人名）
ahora 现在
Enseño 教学（我教）
vuestra 你们的

# 例句
Ricardo es el novio de Rosa. 里卡多是罗莎的男朋友。
Enseño nuestra lengua en una universidad de este país. 我在这个国家的一所大学教我们的语言。
Ella es amiga del profesor González. 她是冈萨雷斯老师的朋友。
La novia del chico se llama Elena. 这个小伙子的女朋友叫艾蕾娜。
¿Dónde están ustedes? 诸位在哪儿？
Ahora estamos en China. 现在我们在中国。
¿Dónde enseñas vuestra lengua? 你在哪里教你们的语言？

# 语法点
介词 DE —— 所属关系
介词 de 的主要用法之一是表示所属。de 后面出现单数阳性定冠词 el 时，缩写为 del。
1,2,3,4
介词 EN —— 地点
介词 en 的主要用法之一是表示地点，用来回答 ¿dónde? 的问题。
5,6,7
```

---

## 一句话

**用户在 `教材/` 下放 `.txt` 文件，程序启动时让用户选一本，整场练习都在该教材内进行。不同教材之间互不污染。语法点可选。**
