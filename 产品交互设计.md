# 西班牙语陪练程序 —— 用户交互设计

> 教材内容直接写死在代码里，用户打开即用。一次性程序，不做通用解析。

---

## 核心设计思路

程序不做自动裁判。Whisper 识别中西双语（尤其初学者发音）不可靠，不用 NLP/LLM 也无法可靠地指出"哪里错了"。

所以程序的角色是：**念给你听 → 逼你开口 → 录下你说的 → 把你说的和正确答案都播给你听 → 你自己判断对不对。**

判罚权在你手里。你按一个键告诉程序"对"或"错"，程序据此安排哪些词多练、哪些词过掉。Whisper 只在部分模式下作为可选辅助——把你说的话转成文字显示在屏幕上，供你参考，但不用它做自动判断。

---

## 打开程序

用户打开程序，看到主菜单：

```
🇪🇸  西班牙语陪练
──────────────────────────
  [1] 听西语说中文
  [2] 听中文说西语
  [3] 听写（键盘拼写）
  [4] 跟读
  [5] 混着来
  [G] 语法讲解
  [Q] 退出
```

按一个键，练习就开始。

---

## 生词练习（模式 1 和 2）

模式 1（听西说中）和模式 2（听中说西）是镜像关系，交互流程一样。以模式 1 为例。

### 一个词的练习流程

1. 程序用西语念一个词，比如 "novio"
2. 发出"嘀"一声，麦克风打开，开始录音
3. 你对着麦克风说答案（中文意思）
4. 你按 Enter 结束录音（或者说完了自动检测静音结束）
5. 程序做两件事：
   - **播放你刚才的录音**，让你听到自己说了什么
   - **念出正确答案**："novio，男朋友"
6. 程序问你："对不对？"——你按 Y（对）或 N（错）
7. 如果你按 N，这个词排到队尾，等会儿再碰到
8. 如果你按 Y，程序记录一次正确。连续两次 Y，这个词本轮不再出现

### 为什么这样设计

你听到自己的录音，立刻就知道自己说得怎么样。如果发音含糊、意思说错了，自己的耳朵比任何算法都诚实。程序不需要告诉你"你错了"——你自己听得出来。

### 可选：Whisper 辅助显示

可以在设置里打开"屏幕字幕"：程序用 Whisper 把你刚才说的话转写成文字，打印在屏幕上。这只是一个参考——你是新手，发音可能不准，Whisper 转写也可能不准。它不参与判罚，只供你瞄一眼。

默认关闭。用户不需要它也能完成全部练习。

### 按键

| 按键 | 效果 |
|---|---|
| Y | 答对了 |
| N | 答错了（词回到队尾） |
| S | 跳过，不计数，排到队尾 |
| R | 重听一遍原词发音 |
| D | 翻转方向（听西说中 ↔ 听中说西） |
| Q | 退出，回到主菜单 |

---

## 听写模式（模式 3）

程序念一个西语单词，你在键盘上敲出拼写。程序逐字比对，拼对了就过，拼错了排到队尾。

这是唯一由程序自动判罚的模式——键盘输入没有歧义，不需要 Whisper。

---

## 跟读模式（模式 4）

跟读不考意思，练的是嘴。流程如下：

1. 程序念一句西语，比如 "Ricardo es el novio de Rosa."
2. 发出"嘀"一声，开始录音
3. 你跟读
4. 你说完（或按 Enter 结束），程序做一件事：**先播原句，再播你的录音**
5. 原句和你的录音交替播放，你对比听
6. 你自己判断：跟得像不像？按 Y（还行）或 N（差得远）或 R（再听一遍对比）
7. 按 N 的话，可以马上再跟读一次同一句

**这个模式完全不依赖 Whisper。** 它就是一个录音机 + 播放器。你不需要任何人告诉你发音对不对——原句和你的录音交替播放时，差距一目了然（一耳了然）。

---

## 混着来（模式 5）

把模式 1、2、3 随机混合。同一个生词可能这轮让你听西说中、下轮让你听中说西、再下轮让你拼写。

判罚逻辑同上：听写由程序自动判断拼写，其他由你自己按 Y/N。所有按键（Y/N/S/R/D）在混着来中同样有效。

---

## 语法讲解（按 G）

按 G 进入语法浏览。程序列出教材中的语法点，用户选一个：

- 中文语法说明显示在屏幕上（不朗读）
- 西语例句逐条用 TTS 朗读

纯辅助功能，不参与练习循环。

---

## 错误循环规则

无论哪种模式，同一个规则：

- 答错 → 排到队尾，稍后再出现
- 答对一次 → 排到队尾，再巩固一次
- 连续两次答对 → 本轮不再出现
- 练习结束退出程序 → 全部清零，不保留任何记录

---

## 总按键表

| 按键 | 功能 | 适用模式 |
|---|---|---|
| 1-5, G, Q | 主菜单选模式 | 主菜单 |
| Enter | 结束录音 | 1, 2, 4, 5 |
| Y | 我答对了 | 1, 2, 4, 5 |
| N | 我答错了 | 1, 2, 4, 5 |
| S | 跳过 | 全部 |
| R | 重听原音 | 全部 |
| D | 翻转方向 | 1, 2, 5 |
| Q | 退出回菜单 | 全部 |

---

## Whisper 的定位

整个程序中，Whisper 只有一个用途：可选的屏幕字幕。用户可以在设置里开关。打开后，在模式 1 和 2 中，程序会把你说的话转写成文字显示在屏幕上。

它不参与判罚。判罚永远是你自己按 Y 或 N。

这个定位意味着：即使 Whisper 转错了，也不影响练习的正确性。即使你不用 Whisper（或没装），程序的核心功能完全不受影响。

---

## 一句话

**程序念给你听，录下你说的，把你的录音和正确答案都播给你听。你自己判断对不对。你告诉程序对错，程序安排不会的多练几遍。全程不需要算法替你判断——你自己的耳朵就是最好的裁判。**
