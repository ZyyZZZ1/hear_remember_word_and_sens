# 组过渡交互优化方案

## 核心改动原则

> **每个词原有的 P/N/B/R/G/F/Q 菜单保留不变。**
> **只有最后一个词时，往这个菜单里加入组导航选项。**
> **用户自始至终只看到一个菜单。**

---

## 一、改什么

### 不改的部分

每组有 N 个词，前 N-1 个词的交互**完全不变**：

```
屏幕：  [Enter/P]通过  [N]保留  [B]上词  [R]重听  [G]下组  [F]收藏  [Q]退出
用户：  按 P 或 N 或 B 或 R 或 G 或 F 或 Q
         → 跟原来一模一样
```

### 改的部分

**第 N 个词**（最后一个词）时，菜单扩展为：

```
屏幕：  [Enter/P]通过  [N]保留  [B]上词  [R]重听  [S]重练本组  [G]下一组  [F]收藏  [Q]退出
```

比原来多了 `[S]重练本组`，而 P 和 G 在最后一个词语境下含义稍有不同（见下）。

**删除 `_handle_loop_end`**。不再有"循环结束！下一组还是从头开始"这个独立弹窗。它的功能被吸收到上面的菜单里了。

---

## 二、各模式具体交互流程

### 模式1-记忆导入（以一组5个词为例）

**前4个词**（不变）：
```
  屏幕： 当前单词 / 例句
  用户：听完、跟读、打字测验
  屏幕： [Enter/P]通过 [N]保留 [B]上词 [R]重听 [G]下组 [F]收藏 [Q]退出
  用户：按 P（通过）
         → 进入下一个词
```

**第5个词（最后一个）**：
```
  屏幕： 当前单词
  用户：听完、跟读、打字测验
  屏幕： [Enter/P]通过 [N]保留 [B]上词 [R]重听 [S]重练本组 [G]下一组 [F]收藏 [Q]退出
                                                    ↑ 新增
  用户选项：
    P → 通过本词 → 组完成 → 自动递归进下一组（或通关提示后进）
    N → 保留稍后练 → 循环继续（跟原来一样，不触发组过渡）
    B → 上词
    R → 重听
    S → 重置本组，从头再来 ← 替代原来 _handle_loop_end 的"从头开始"
    G → 直接去下一组（跟原来一样）
    F → 收藏
    Q → 退出
```

### 模式1 ES→ZH / 模式2 ZH→ES

同上。`_decision_pnbr` 在最后一词时菜单加 `[S]` 选项。

```
前 N-1 个词：  [Enter/P]通过 [N]保留 [B]上词 [R]重听 [G]下组 [F]收藏 [Q]退出
最后一个词：   [Enter/P]通过 [N]保留 [B]上词 [R]重听 [S]重练本组 [G]下一组 [F]收藏 [Q]退出
```

### 模式1-句子新流程（catch/listen）

catch 分支：抓词结果后无 P/N 菜单（自动判对），但最后一句完成后的逻辑同理。

listen 分支：有 `[Enter/P]通过 [N]保留 [R]重听 [W]拆听 [F]收藏 [Q]退出` 菜单，最后一句时加 `[S]重练本组`。

### 模式4-纯听

有 `[Y]过 [N]留 [R]重听 [W]拆听 [F]收藏 [Q]退出` 菜单，最后一句时：
- Y 替代 P
- 加 `[S]重练本组`

```
最后一句：  [Y]过 [N]留 [R]重听 [W]拆听 [S]重练本组 [F]收藏 [Q]退出
```

### 模式3-单词听写

单词听写没有逐词的 P/N 菜单（是直接输入判对），所以处理方式不同：

```
第 5 个词（最后一个）：
  听到：  "这是最后一个了"  ← TTS 提示
  听到：  [单词读音]
  屏幕：  当前单词：hermano
  用户：  输入 "hermano" → Enter
  屏幕：  [OK] 正确！

  -- 第 N 组通关 --

  [Enter] 继续下一组  [B] 回组菜单  [S] 重练本组  [Q] 退出
                                                  ↑ 新增
  用户：
    Enter → 进下一组
    B → 回组菜单
    S → 重练本组
    Q → 退出
```

这已经是**唯一的菜单**，不需要再按一次 Enter 来触发它。

---

## 三、不改的模式

- 模式3-句子听写：for 循环直接进下一组，无多余菜单
- 模式4-听写抓词：for 循环直接进下一组，无多余菜单
- 模式5-混着来：for 循环直接进下一组，无多余菜单

---

## 四、删什么

**`_handle_loop_end` 函数**（1956 行）不再需要，因为它的功能被最后一句的扩展菜单吸收了。

---

## 五、实施顺序

| 阶段 | 内容 | 改动量 |
|---|---|---|
| 1 | 模式3-单词听写 + TTS 提示 | 小（独立函数） |
| 2 | `_decision_pnbr` 最后一项时扩展菜单 + 跳过 `_handle_loop_end` | 中（影响 ES→ZH、ZH→ES） |
| 3 | 记忆导入 最后一项时扩展菜单 + 跳过 `_handle_loop_end` | 中 |
| 4 | 句子新流程 + 纯听 最后一项处理 | 中 |
| 5 | 清理 `_handle_loop_end` | 小 |
