# 西班牙语陪练程序 —— 测试用例

> 所有测试从外部使用者视角进行。不检查内部变量、不修改内部状态、不读代码。测试者扮演一个普通用户，按描述操作程序，观察程序表现。
>
> **自动化标注说明：**
> - `[自动]` —— 脚本可全自动执行并判绿。纯键盘/屏幕输出验证，不依赖音频
> - `[手动]` —— 核心验证点依赖音频 I/O（TTS 朗读、麦克风录音、回放），脚本无法替人判断，由人按测试步骤操作并观察

---

## 测试环境

- 一台 Windows 电脑，已安装程序所需依赖
- 麦克风可用
- 扬声器或耳机可用
- 测试者不需要懂西班牙语（但需要知道几个西语单词的读音以便模拟回答）

---

## 一、主菜单

### TC-01：启动程序 `[自动]`

- **测试内容**：程序能否正常启动并显示主菜单
- **测试目的**：验证启动流程正常
- **测试过程**：
  1. 在终端中启动程序
- **预期结果**：
  - 终端显示程序标题"西班牙语陪练"
  - 显示模式选项列表（1 到 5、G、Q）
  - 光标等待用户输入
- **自动化说明**：纯终端输出验证，无音频依赖。脚本启动进程 → 读 stdout → 断言标题和选项列表存在。

### TC-02：主菜单选模式 `[自动]`

- **测试内容**：主菜单各按键是否进入对应模式
- **测试目的**：验证主菜单按键映射正确
- **测试过程**：
  1. 启动程序
  2. 依次按 1、等待进入后按 Q 退出回菜单
  3. 依次按 2、Q 退出
  4. 依次按 3、Q 退出
  5. 依次按 4、Q 退出
  6. 依次按 5、Q 退出
  7. 按 G、Q 退出
- **预期结果**：
  - 按 1 后进入"听西语说中文"模式（可听到西语朗读或看到练习界面）
  - 按 2 后进入"听中文说西语"模式
  - 按 3 后进入"听写"模式
  - 按 4 后进入"跟读"模式
  - 按 5 后进入"混着来"模式
  - 按 G 后进入语法讲解界面
  - 每次按 Q 都能回到主菜单
- **自动化说明**：预期结果里明确写了"或看到练习界面"——不要求验证音频。脚本验证每次按键后屏幕文字变化、Q 回到主菜单即可。

### TC-03：主菜单按 Q 退出 `[自动]`

- **测试内容**：在主菜单按 Q 是否退出程序
- **测试目的**：验证退出功能
- **测试过程**：
  1. 启动程序
  2. 在主菜单按 Q
- **预期结果**：
  - 程序退出，回到终端命令行
- **自动化说明**：脚本启动进程 → 发送 Q → 验证进程退出码和 stdout 无异常。

---

## 二、模式 1：听西语说中文

### TC-04：正常流程——听到西语后说出中文、自判为对 `[手动]`

- **测试内容**：完整走一遍"听西语说中文"的正常流程
- **测试目的**：验证模式 1 的核心交互链路
- **测试过程**：
  1. 主菜单按 1
  2. 听到程序用西语朗读一个单词（如"novio"）
  3. 听到提示音后，对着麦克风说"男朋友"
  4. 按 Enter 结束录音
  5. 听到程序播放刚才自己的录音
  6. 听到程序朗读正确西语答案，屏幕显示中文释义
  7. 按 Y
- **预期结果**：
  - 程序朗读西语清晰可辨
  - 提示音后麦克风打开（可通过说话时屏幕有反馈得知）
  - 录音回放可听到自己说的内容
  - 正确西语答案被朗读
  - 按 Y 后进入下一题（朗读下一个词或句子）

> **回应你的意见**：你说得对，这个用例脚本无法自动完成。脚本不能"听到" TTS 朗读是否正确，也不能"听到"录音回放是否清晰。这是一个纯手动测试用例——人坐在电脑前用耳朵判断。脚本能做的仅限于：启动程序、按 1 进入模式 1、检测程序不崩溃。其余靠人。

### TC-05：自判为错——词回到队尾 `[手动]`

- **测试内容**：按 N 后，同一个词是否稍后再次出现
- **测试目的**：验证答错回队尾的循环逻辑
- **测试过程**：
  1. 进入模式 1
  2. 听到第一个词后，录音回答
  3. 按 N
  4. 继续后续答题（按 Y 快速通过其他题）
  5. 观察之前按 N 的那个词是否在若干题后再次出现
- **预期结果**：
  - 按 N 后程序不报错、不卡死，正常进入下一题
  - 之前答错的那个词在本轮后面再次出现（可能不是立即，但不会消失）

> **回应**：回队尾逻辑本身可测，但触发条件（听词 → 录音 → 按 N）依赖音频。转为手动。

### TC-06：连续两次答对后不再出现 `[手动]`

- **测试内容**：同一个词连续两次 Y 后是否彻底消失
- **测试目的**：验证通过条件
- **测试过程**：
  1. 进入模式 1
  2. 留意某个词第一次出现时按 Y
  3. 该词第二次出现时再按 Y
  4. 继续练到所有词都通过（程序提示本轮结束或自动回到主菜单）
  5. 观察该词是否在第二次 Y 之后又出现了第三次
- **预期结果**：
  - 同一个词第一次 Y 后会再次出现一次（巩固）
  - 第二次 Y 后该词不再出现
  - 所有词通过后程序给出结束提示，回到主菜单

> **回应**：计数逻辑简单，但全流程依赖音频交互。转为手动。

### TC-07：答错一次后连续两次 Y 也应通过 `[手动]`

- **测试内容**：先 N 再 Y Y 是否能正常通过
- **测试目的**：验证错误计数被正确重置
- **测试过程**：
  1. 进入模式 1
  2. 某词第一次出现时按 N
  3. 该词再次出现时按 Y
  4. 再出现时按 Y
  5. 观察后续是否还出现
- **预期结果**：
  - 按 N 后该词回到队尾重新排队
  - 重新出现后连续两次 Y，该词不再出现

> **回应**：同 TC-06，转为手动。

### TC-08：R 键重听 `[手动]`

- **测试内容**：在答题前按 R 是否能重听原词
- **测试目的**：验证 R 键功能
- **测试过程**：
  1. 进入模式 1
  2. 听到一个词后，不要开始说话，直接按 R
  3. 再按一次 R
- **预期结果**：
  - 每次按 R 后，程序重新朗读当前这个词的西语发音
  - 朗读结束后程序继续等待用户录音（不会跳到下一题）

> **回应你的意见**：TTS 是否正常播放声音——脚本无法验证。脚本只能做到：按键 R 后程序不崩溃、不跳到下一题（判断当前状态未改变）。声音对不对，人耳朵判断。转手动。

### TC-09：S 键跳过 `[自动]`

- **测试内容**：按 S 跳过当前词，该词稍后是否再次出现
- **测试目的**：验证跳过功能
- **测试过程**：
  1. 进入模式 1
  2. 听到一个词后，按 S
  3. 继续练习，观察该词是否再次出现
- **预期结果**：
  - 按 S 后立即跳到下一个词（不录音、不判对错）
  - 被跳过的词在后续练习中再次出现

> **回应**：改在模式 3（听写）中验证——纯键盘交互。脚本进模式 3 → S → 断言进入下一词、被跳过词后续再次出现。无音频依赖，标记为自动。

### TC-10：D 键翻转方向 `[手动]`

- **测试内容**：在模式 1 中按 D 是否切换到模式 2
- **测试目的**：验证方向翻转
- **测试过程**：
  1. 进入模式 1（听西说中）
  2. 练一两题后，按 D
  3. 观察下一题的行为变化
- **预期结果**：
  - 按 D 后，下一题变成"听中文说西语"（程序朗读中文，期待用户说西语）
  - 再按 D 又能切回来

> **回应**：方向变化只能靠耳朵听 TTS 语言来判断，脚本无屏幕文字可抓。转手动。

### TC-11：练习中按 Q 返回主菜单 `[自动]`

- **测试内容**：练习中途按 Q 是否回到主菜单
- **测试目的**：验证退出功能
- **测试过程**：
  1. 进入模式 1
  2. 练一两题后按 Q
- **预期结果**：
  - 立即回到主菜单，显示模式选项
- **自动化说明**：Q 键在任意状态下应可用，脚本验证按键后回到主菜单 stdout。不验证音频。

### TC-12：静音超时自动结束录音 `[手动]`

- **测试内容**：用户不说话，程序是否在静音一段时间后自动结束录音
- **测试目的**：验证静音检测
- **测试过程**：
  1. 进入模式 1
  2. 听到提示音后，不对麦克风说话，保持安静
  3. 等待
- **预期结果**：
  - 若干秒（如 3-5 秒）后程序自动结束录音
  - 然后播放正确答案，等待用户按 Y/N
  - 程序不卡死

> **回应**：依赖真实麦克风和静音检测。脚本无法模拟"不说话"。转手动。

### TC-13：所有词通过后本轮结束 `[手动]`

- **测试内容**：全部答完后程序如何提示
- **测试目的**：验证一轮结束的体验
- **测试过程**：
  1. 进入模式 1
  2. 对每个出现的词连续两次按 Y（使其通过）
  3. 直到没有更多词
- **预期结果**：
  - 程序提示本轮结束（如"本轮结束"提示音或文字）
  - 自动回到主菜单
- **自动化说明**：如果有屏幕文字提示"本轮结束"，脚本可验证；但整个到达终态的过程依赖音频交互。转手动。

---

## 三、模式 2：听中文说西语

### TC-14：正常流程——听到中文后说出西语 `[手动]`

- **测试内容**：模式 2 的基本交互链路
- **测试目的**：验证方向翻转后的流程正常
- **测试过程**：
  1. 主菜单按 2
  2. 观察程序如何呈现中文（屏幕显示或 TTS 朗读）
  3. 对着麦克风说对应的西语
  4. 按 Enter 结束录音
  5. 回放录音 → 播放正确西语答案
  6. 按 Y
- **预期结果**：
  - 中文问题以屏幕文字或 TTS 形式呈现
  - 录音回放正常
  - 正确答案以西语 TTS 朗读
  - 按 Y 后进入下一题

> **回应**：同 TC-04，全程依赖音频。转手动。

### TC-15：D 键翻转回模式 1 `[手动]`

- **测试内容**：在模式 2 中按 D 是否切回模式 1
- **测试目的**：验证 D 键双向翻转
- **测试过程**：
  1. 进入模式 2
  2. 按 D
  3. 观察下一题是否变成听西说中
- **预期结果**：
  - 下一题程序朗读西语，期待用户说中文

> **回应**：同 TC-10，听 TTS 语言辨方向，转手动。

---

## 四、模式 3：听写

### TC-16：拼写正确——通过 `[自动]`

- **测试内容**：输入正确拼写后程序判定通过
- **测试目的**：验证听写自动判罚的正确性
- **测试过程**：
  1. 主菜单按 3
  2. 听到程序朗读一个西语单词（如"novio"）
  3. 在键盘上输入 "novio"，按 Enter
- **预期结果**：
  - 程序提示"对"
  - 进入下一个单词
- **自动化说明**：纯键盘交互。脚本进入模式 3 → 等待单词提示（可检测屏幕文字输出）→ 输入正确拼写 → 断言屏幕出现"对" → 进入下一题。TTS 朗读不参与验证。

### TC-17：拼写错误——回队尾 `[自动]`

- **测试内容**：输入错误拼写后程序指出正确拼写并回队尾
- **测试目的**：验证错误反馈和循环
- **测试过程**：
  1. 进入模式 3
  2. 听到一个单词后，故意输入错误拼写（如听到"novio"输入"novia"），按 Enter
  3. 观察反馈
  4. 继续答题，观察该词是否再次出现
- **预期结果**：
  - 程序显示正确拼写
  - 程序逐个字母朗读正确拼写
  - 该词稍后再次出现
- **自动化说明**：键盘交互 + 屏幕输出。脚本可验证：输入错误拼写 → 屏幕出现正确拼写 → 该词在后续再次出现。TTS 逐个字母朗读是音频行为，脚本不验证，人耳确认即可。

### TC-18：连续两次拼写正确后通过 `[自动]`

- **测试内容**：同一词连续两次正确拼写后不再出现
- **测试目的**：验证通过条件
- **测试过程**：
  1. 进入模式 3
  2. 正确拼写某个词两次
  3. 观察是否第三次出现
- **预期结果**：
  - 第一次正确拼写后该词再次出现一次
  - 第二次正确拼写后该词不再出现
- **自动化说明**：纯键盘交互，无音频依赖。脚本可完整验证同一词的两次出现 → 两次正确拼写 → 不再出现。

### TC-19：R 键重听 `[手动]`

- **测试内容**：在听写模式中按 R 重听单词
- **测试目的**：验证 R 键
- **测试过程**：
  1. 进入模式 3
  2. 听到单词后，不输入，按 R
- **预期结果**：
  - 程序重新朗读当前单词

> **回应**：同 TC-08。TTS 声音是否正确，脚本无法验证。转手动。

### TC-20：S 键跳过 `[自动]`

- **测试内容**：按 S 跳过，该词回到队尾
- **测试目的**：验证跳过功能
- **测试过程**：
  1. 进入模式 3
  2. 按 S
  3. 继续练习，观察被跳过的词是否再次出现
- **预期结果**：
  - 跳过当前词，进入下一个
  - 被跳过的词后续再次出现
- **自动化说明**：纯键盘交互。脚本验证：S → 进入下一词；该词在后续再次出现。无音频依赖。

---

## 五、模式 4：跟读

### TC-21：正常跟读流程 `[手动]`

- **测试内容**：完整走一遍跟读流程
- **测试目的**：验证跟读模式的核心链路
- **测试过程**：
  1. 主菜单按 4
  2. 听到程序朗读一句西语
  3. 提示音后跟读
  4. 按 Enter 结束录音
  5. 仔细听播放内容——是否先播原句、再播自己的录音
  6. 按 Y
- **预期结果**：
  - 程序先播放原句录音，再播放用户刚才的录音
  - 两段录音交替播放，可以对比
  - 按 Y 后进入下一句

> **回应你的意见**：录音是否正常启动、回放是否有声音——这些脚本做不到。跟读模式的核心价值（原句 vs 录音对比）完全依赖音频，转手动。

### TC-22：跟读不满意（N）后立即重试 `[手动]`

- **测试内容**：按 N 后是否能立即再跟读同一句
- **测试目的**：验证跟读模式的重试逻辑
- **测试过程**：
  1. 进入模式 4
  2. 跟读一句后按 N
  3. 观察接下来发生什么
- **预期结果**：
  - 程序重新播放同一句原句
  - 用户可以立即再跟读一次（不需要排队到后面）
  - 同一句跟读满意（按 Y）两次后不再出现

> **回应**：逻辑本身可测，但触发条件（跟读 + 录音 + 回放）依赖音频，转手动。

### TC-23：R 键重新对比 `[手动]`

- **测试内容**：在跟读对比阶段按 R 是否重新播放对比
- **测试目的**：验证对比重放
- **测试过程**：
  1. 进入模式 4
  2. 跟读完一句后，在等待 Y/N 判断时按 R
  3. 再按一次 R
- **预期结果**：
  - 每次按 R，程序重新交替播放原句和用户录音

> **回应**：音频行为的验证，转手动。

### TC-24：跟读模式不依赖 Whisper `[手动]`

- **测试内容**：确认跟读模式在无 Whisper 环境下正常工作
- **测试目的**：验证跟读模式与 Whisper 解耦
- **测试过程**：
  1. 如果之前开启了 Whisper 字幕，先关闭
  2. 进入模式 4
  3. 正常跟读并完成一轮
- **预期结果**：
  - 跟读流程完整可用，不受 Whisper 开关影响
  - 录音、播放对比、Y/N 判断均正常

> **回应**：跟读全流程依赖音频，转手动。

---

## 六、模式 5：混着来

### TC-25：混合模式包含多种题型 `[手动]`

- **测试内容**：混着来模式中是否出现不同类型的题目
- **测试目的**：验证混合随机的正确性
- **测试过程**：
  1. 主菜单按 5
  2. 连续练 10 题以上
  3. 观察每道题的表现形式
- **预期结果**：
  - 有的题是听西语说中文（TTS 念西语）
  - 有的题是听中文说西语（显示中文或 TTS 念中文）
  - 有的题是听写（等待键盘输入拼写）
  - 题型顺序不固定，随机出现

> **回应**：区分"当前是什么题型"依赖人观察（听 TTS 还是看键盘提示），转手动。

### TC-26：混合模式中听写题自动判罚 `[手动]`

- **测试内容**：混着来中的听写题是否仍由程序自动判罚
- **测试目的**：验证听写题在混合模式中保持自动判罚
- **测试过程**：
  1. 进入模式 5
  2. 当遇到听写题时（等待键盘输入），输入正确拼写
  3. 当遇到非听写题时（等待录音），录音后按 Y/N
- **预期结果**：
  - 听写题输入后程序自动提示对错，不需要按 Y/N
  - 非听写题仍需要用户按 Y/N

> **回应**：需要先识别当前是哪种题型，转手动。

### TC-27：混合模式中 D 键不作用于听写题 `[手动]`

- **测试内容**：在听写题中按 D 是否无效果
- **测试目的**：验证 D 键在听写题中的行为
- **测试过程**：
  1. 进入模式 5
  2. 遇到听写题时按 D
- **预期结果**：
  - 按 D 无效果，或程序忽略（因为听写题没有方向可翻转）
  - 遇到非听写题时 D 正常翻转方向

> **回应**：需要区分题型，转手动。

### TC-28：各题型独立计数 `[手动]`

- **测试内容**：每个词/句在各题型中的通过计数是否独立
- **测试目的**：验证同一词在不同题型中的计数逻辑
- **测试过程**：
  1. 进入模式 5
  2. 留意某个词在听西说中题型中出现并答对
  3. 该词稍后在听写题型中出现
  4. 故意拼写错误
  5. 观察该词后续行为
- **预期结果**：
  - 同一个词在不同题型中的通过计数互不影响
  - 听西说中题型中通过不代表听写题型中也通过

> **回应**：多题型跨模式跟踪复杂，依赖人全程观察，转手动。

---

## 七、语法讲解（G）

### TC-29：语法点列表展示 `[自动]`

- **测试内容**：按 G 后是否显示语法点列表
- **测试目的**：验证语法讲解入口
- **测试过程**：
  1. 主菜单按 G
- **预期结果**：
  - 屏幕列出所有语法点（如"介词 DE —— 所属关系"、"介词 EN —— 地点"）
  - 每个语法点有编号
  - 提示用户按数字选择
- **自动化说明**：纯屏幕输出验证。脚本按 G → 读 stdout → 断言语法点列表存在。

### TC-30：选择一个语法点——显示说明并朗读例句 `[手动]`

- **测试内容**：选一个语法点后是否展示说明并朗读例句
- **测试目的**：验证语法讲解内容
- **测试过程**：
  1. 按 G 进入语法列表
  2. 按 1 选择第一个语法点
- **预期结果**：
  - 屏幕显示该语法点的中文说明
  - 程序开始用 TTS 朗读第一条西语例句
  - 可以听到清晰可辨的西语句子

> **回应**：屏幕中文说明脚本可验证，但 TTS 朗读例句是否正确只能人耳确认。转手动。

### TC-31：R 键重听例句、Q 键返回 `[手动]`

- **测试内容**：语法讲解中的导航按键
- **测试目的**：验证语法模式内的导航
- **测试过程**：
  1. 进入某个语法点
  2. 在例句朗读过程中或朗读后按 R
  3. 按 Q
- **预期结果**：
  - 按 R 后重新朗读当前例句
  - 按 Q 后返回语法点列表（不是直接回主菜单）
  - 在语法点列表再按 Q 回到主菜单

> **回应**：R 键触发的是 TTS 音频重播，脚本验证不了。转手动。

---

## 八、录音与回放

### TC-32：录音回放声音清晰可辨 `[手动]`

- **测试内容**：录下的声音回放时是否清晰
- **测试目的**：验证录音和回放的基础质量
- **测试过程**：
  1. 进入模式 1
  2. 听到提示音后，用正常音量说一句完整中文
  3. 按 Enter 结束
  4. 仔细听回放
- **预期结果**：
  - 回放声音可清晰辨认说了什么
  - 回放音量适中
  - 没有严重噪音或失真

> **回应你的意见**：这个脚本完全做不到。"清晰可辨"是人耳的判断标准，不是程序的。转手动。

### TC-33：录音文件不残留 `[自动]`

- **测试内容**：退出程序后录音临时文件是否被清理
- **测试目的**：验证录音不落盘
- **测试过程**：
  1. 启动程序，进入模式 1，练几题（产生录音）
  2. 按 Q 退出程序
  3. 检查程序所在目录是否有残留的音频文件
- **预期结果**：
  - 程序目录下没有残留的 wav/mp3 等录音文件
- **自动化说明**：脚本启动程序 → 跑几题 → 退出 → 扫描目录 glob('*.wav', '*.mp3') → 断言为空。录音过程本身依赖音频，但验证点是文件系统的清理行为，不依赖音频判断。

---

## 九、Whisper 字幕（可选功能）

### TC-34：Whisper 字幕默认关闭 `[手动]`

- **测试内容**：首次启动程序时 Whisper 字幕是否默认关闭
- **测试目的**：验证 Whisper 不是默认开启
- **测试过程**：
  1. 首次启动程序
  2. 进入模式 1，录音回答
  3. 观察屏幕上是否出现转写文字
- **预期结果**：
  - 屏幕不显示 Whisper 转写文字
  - 练习流程完全不受影响

> **回应**：进模式 1 并录音后才触发字幕逻辑，全链路依赖音频交互。转手动。

### TC-35：打开 Whisper 字幕——显示转写结果 `[手动]`

- **测试内容**：开启字幕后是否显示转写文字
- **测试目的**：验证 Whisper 字幕功能
- **测试过程**：
  1. 在设置中开启 Whisper 字幕
  2. 进入模式 1
  3. 录音说中文答案
  4. 观察屏幕
- **预期结果**：
  - 录音结束后，屏幕上出现一段文字（Whisper 转写的内容）
  - 转写文字显示在回放和正确答案之前或同时
  - 转写结果不影响 Y/N 判罚流程

> **回应你的意见**：脚本怎么录音、怎么发出可被 Whisper 转写的声音、怎么被录下来——这些都不可自动化。转手动。测试时人对着麦克风说话，Whisper 在屏幕上出什么字，人眼看。

### TC-36：未安装 Whisper 时程序正常运行 `[手动]`

- **测试内容**：没有安装 faster-whisper 时程序是否正常
- **测试目的**：验证 Whisper 是真正的可选依赖
- **测试过程**：
  1. 在未安装 faster-whisper 的环境中启动程序
  2. 进入模式 1，完成一轮完整练习
  3. 进入模式 2、3、4、5，各练几题
- **预期结果**：
  - 程序正常启动，不报错
  - 所有模式正常可用
  - 尝试开启 Whisper 字幕时程序友好提示"Whisper 未安装"

> **回应**："完成一轮完整练习"依赖音频交互，全部模式都需要人操作验证。转手动。

---

## 十、持久化验证

### TC-37：退出后重新启动——练习记录清零 `[自动]`

- **测试内容**：退出程序再启动，之前的练习进度是否消失
- **测试目的**：验证无持久化
- **测试过程**：
  1. 启动程序，进入模式 1
  2. 练到一半（不要全部通过），按 Q 退出
  3. 重新启动程序，进入模式 1
  4. 观察是否从零开始（之前已通过的词是否再次出现）
- **预期结果**：
  - 重新启动后，之前通过的词会再次出现
  - 所有词都从零计数开始
  - 不存在"上次练到哪了"的状态恢复
- **自动化说明**：用模式 3（听写）而非模式 1：脚本启动 → 模式 3 → 正确拼写使一些词通过 → 记录已通过词列表 → Q 退出 → 重新启动 → 模式 3 → 断言之前通过的词再次出现。纯键盘交互，无音频依赖。

---

## 汇总

| 类型 | 数量 | 用例 |
|---|---|---|
| `[自动]` | 12 | TC-01, TC-02, TC-03, TC-09, TC-11, TC-16, TC-17, TC-18, TC-20, TC-29, TC-33, TC-37 |
| `[手动]` | 25 | TC-04, TC-05, TC-06, TC-07, TC-08, TC-10, TC-12, TC-13, TC-14, TC-15, TC-19, TC-21, TC-22, TC-23, TC-24, TC-25, TC-26, TC-27, TC-28, TC-30, TC-31, TC-32, TC-34, TC-35, TC-36 |

**自动化测试只做 12 条 `[自动]` 用例。** 其余由你来跑。后期如果程序提供了"无头模式"（跳过 TTS/录音，直接暴露内部状态），部分手动用例可升级为自动。

---

> **意见回应：半自动怎么跟人来交互？**
>
> 你问得对。"半自动"不是说脚本和人实时交互——实际上脚本跑脚本的，人测人的，各不相干。**"半自动"这个分类对 MVP 没有实操意义，反而制造困惑。**
>
> 务实方案是**只分两档**，把之前标为 `[半自动]` 的 7 条拆掉：
>
> | 原半自动用例 | 拆入 | 原因 |
> |---|---|---|
> | TC-09 S键跳过 | **自动** | 改在模式 3（纯键盘）测 S 键逻辑，绕过音频 |
> | TC-10 D键翻转 | **手动** | 方向变化只能靠耳朵听 TTS 语言，脚本无屏幕文字可抓 |
> | TC-15 D键翻转回 | **手动** | 同上 |
> | TC-30 语法点例句朗读 | **手动** | TTS 朗读是否正确，人耳判断 |
> | TC-31 R重听/Q返回 | **手动** | R 键触发的是音频重播，脚本验证不了 |
> | TC-34 字幕默认关闭 | **手动** | 进模式 1 并录音后才触发字幕逻辑，全链路依赖音频 |
> | TC-36 无Whisper正常运行 | **手动** | "完成一轮完整练习"依赖音频交互 |
>
> **最终结论：只存在 `[自动]` 和 `[手动]` 两档。** 没有"半自动"——不存在脚本跑一半弹出对话框说"请你听一下这个声音对不对"这种交互方式。脚本能做的全自动做，做不到的步骤人来做。