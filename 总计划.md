# 总计划：西班牙语陪练程序 —— 开发 + 自动化测试 + 测试报告

---

## 依据文档

- `产品交互设计.md`（已审批）
- `需求规格.md`（已审批）
- `测试用例.md`（已审批，含 12 条 `[自动]` 用例）
- `教材`（已提供，作为程序内置数据源）

---

## 总目标

1. 按需求规格和交互设计编写出可运行的程序
2. 编写自动化测试脚本，覆盖 12 条 `[自动]` 用例
3. 执行测试，生成 `test_report.md`

**任务结束标志：** 提交 `test_report.md`。

---

# 第一阶段：编写程序

## 1.1 程序形态

终端运行的 Python 3 程序，Windows 环境。教材数据写死在代码中。不联网，不持久化。

## 1.2 必须实现的模式

| 按键 | 模式 | 核心行为 |
|------|------|----------|
| 1 | 听西语说中文 | TTS 念西语词/句 → 录音用户说中文 → 回放录音 → 播放正确答案 → 用户按 Y/N 自判 |
| 2 | 听中文说西语 | 显示中文 / TTS 念中文 → 录音用户说西语 → 回放录音 → 播放正确答案 → 用户按 Y/N 自判 |
| 3 | 听写 | TTS 念西语单词 → 用户键盘输入拼写 → 程序自动比对判对错 |
| 4 | 跟读 | TTS 念西语句子 → 录音用户跟读 → 交替播放原句和录音 → 用户按 Y/N 自判 |
| 5 | 混着来 | 模式 1/2/3 随机混合 |
| G | 语法讲解 | 列出语法点 → 用户选一个 → 显示说明 + TTS 朗读例句 |

## 1.3 错误循环规则（全局）

- 答错 → 排到队尾，稍后再出现
- 答对一次 → 排到队尾，巩固一次
- 连续两次答对 → 本轮不再出现
- 跳过（S 键）→ 排到队尾，不计对错
- 程序退出 → 全部清零

## 1.4 自动化测试对程序的硬性要求

为了让 12 条自动化用例能够执行，程序必须满足以下约定：

| 编号 | 约定 | 影响哪些用例 |
|------|------|-------------|
| A | 启动后在 stdout 输出标题和菜单（含 "西班牙语陪练" 文字、选项 1/2/3/4/5/G/Q） | TC-01, TC-02, TC-03 |
| B | 进入每种模式后，stdout 必须输出一行标明当前模式的文字 | TC-02 |
| C | 模式 3（听写）必须在 stdout 展示当前要拼写的单词 | TC-09, TC-16, TC-17, TC-18, TC-20, TC-37 |
| D | 模式 3 拼写正确后 stdout 输出"对"或等效文字提示 | TC-16, TC-17 |
| E | 模式 3 拼写错误后 stdout 输出正确拼写 | TC-17 |
| F | 按 G 后 stdout 列出语法点名称和编号 | TC-29 |
| G | 录音产生的临时文件在退出时必须清理 | TC-33 |
| H | Q 键在整个程序中可用：练习中 Q 返回主菜单，主菜单中 Q 退出程序 | TC-03, TC-11 |

> 如果程序不满足 A~H，受影响的自动化用例将标记为"跳过（程序 stdout 不足）"而非"通过"或"失败"。

## 1.5 教材数据（从 `教材` 文件提取，直接写死进程序）

以下内容从 `教材` 文件中逐条提取，作为程序的唯一数据源。词汇、例句、语法点与程序模式之间的关联关系也一并列出，供审阅。

---

### 生词表（11 个，去重后）

| 序号 | 西语 | 中文释义 |
|------|------|----------|
| 1 | Ricardo | （人名）里卡多 |
| 2 | Rosa | （人名）罗莎 |
| 3 | nuestra / nuestro | 我们的 |
| 4 | lengua | 语言 |
| 5 | universidad | 大学 |
| 6 | este / esta | 这个 |
| 7 | país | 国家 |
| 8 | del（de + el 缩写） | ……的 |
| 9 | González | （人名）冈萨雷斯 |
| 10 | ahora | 现在 |

> 此表去掉了 `教材` 文件中重复出现的词。另移除了专有名词 China（不属于语言学习词汇）。（nuestra、lengua、universidad、este、país、del 各出现了两次）。`nuestra` 和 `este` 保留原型，实际程序中存储其阳性/阴性形式。

**关联模式：** 模式 1、2、3、5 均从此表抽取单词。

---

### 例句表（7 条）

| 序号 | 西语 | 中文翻译 | 所属语法点 |
|------|------|----------|-----------|
| 1 | Ricardo es el novio de Rosa. | 里卡多是罗莎的男朋友。 | 介词 DE —— 所属 |
| 2 | Enseño nuestra lengua en una universidad de este país. | 我在这个国家的一所大学教我们的语言。 | 介词 DE + 介词 EN |
| 3 | Ella es amiga del profesor González. | 她是冈萨雷斯老师的朋友。 | 介词 DE（de+el → del） |
| 4 | La novia del chico se llama Elena. | 这个小伙子的女朋友叫艾蕾娜。 | 介词 DE（de+el → del） |
| 5 | ¿Dónde están ustedes? | 诸位在哪儿？ | 介词 EN —— 地点 |
| 6 | Ahora estamos en China. | 现在我们在中国。 | 介词 EN —— 地点 |
| 7 | ¿Dónde enseñas vuestra lengua? | 你在哪里教你们的语言？ | 介词 EN —— 地点 |

**关联模式：** 模式 1、2、4、5 均从此表抽取句子。

---

### 语法点表（2 个）

#### 语法点 1：介词 DE —— 所属关系

- **说明：** 介词 de 的主要用法之一是表示所属，用来回答 ¿de quién?（谁的）、¿de qué?（什么的）等提问。de 后面出现单数阳性定冠词 el 时，应当缩写为 del。
- **关联例句（序号对应例句表）：** 1、2、3、4

#### 语法点 2：介词 EN —— 地点

- **说明：** 介词 en 的主要用法之一是表示地点，用来回答以 ¿dónde?（在哪儿）提出的问题。
- **关联例句（序号对应例句表）：** 5、6、7（例句 2 同时涉及 de 和 en）

**关联模式：** 按 G 键进入语法讲解后，列出这两个语法点供用户选择。

---

### 教材数据的程序存储格式约定

程序中生词、例句、语法点各自用一个 Python list/dict 存储。例句和语法点之间通过序号引用关联。程序不解析外部文件，数据直接写在代码中。

---

# 第二阶段：编写自动化测试脚本

## 2.1 测试范围

12 条 `[自动]` 用例：TC-01, TC-02, TC-03, TC-09, TC-11, TC-16, TC-17, TC-18, TC-20, TC-29, TC-33, TC-37。

> TC-09 和 TC-20 合并为一个测试函数，报告中分列两条记录。

## 2.2 测试脚本的合法通道

脚本只能通过以下方式与被测程序交互：

| 通道 | 方向 | 对应测试过程 |
|------|------|-------------|
| stdin | 脚本 → 程序 | 模拟按键 |
| stdout | 程序 → 脚本 | 验证预期结果的屏幕文字 |
| 退出码 | 程序 → 脚本 | 验证程序正常退出 |
| 文件系统 | 脚本读取 | 验证残留文件（仅 TC-33） |

**脚本禁止：** 读取程序内部变量、调用程序内部函数、访问程序内存。

## 2.3 防止造假的机制

| 机制 | 说明 |
|------|------|
| 断言挂文档 | 每条断言代码的注释标注对应的测试用例条款号 |
| stdout 原文取证 | 报告中粘贴程序真实 stdout，不靠脚本转述 |
| 输入来自输出 | 拼写测试中，脚本输入的内容从程序 stdout 读取，不自己编 |
| baseline 对比 | TC-33 用"程序运行前后目录差异"而非"目录为空" |
| 独立进程验证 | TC-37 两轮启动完全独立的进程，禁止共享内存 |
| 预期值来自文档 | 语法点清单等预期值直接取自上面 §1.5 的生词表/语法点表，不动态生成 |

## 2.4 文件结构

```
test_runner.py          # 主入口，依次执行各用例，生成 test_report.md
conftest.py             # 公共工具：启停进程、按键注入、stdout 读取、断言函数
test_tc01.py            # TC-01
test_tc02.py            # TC-02
test_tc03.py            # TC-03
test_tc0911.py          # TC-09, TC-11, TC-20
test_tc161718.py        # TC-16, TC-17, TC-18
test_tc29.py            # TC-29
test_tc33.py            # TC-33
test_tc37.py            # TC-37
```

## 2.5 每条用例的测试逻辑摘要

| 用例 | 测试逻辑 |
|------|----------|
| TC-01 | 启动进程 → 读 stdout → 断言含标题和菜单项 → 断言进程未退出 |
| TC-02 | 依次按 1/2/3/4/5/G → 断言 stdout 出现对应模式文字 → 按 Q → 断言回主菜单 |
| TC-03 | 主菜单按 Q → 断言退出码为 0，stdout 无 crash 堆栈 |
| TC-09/20 | 模式 3 → 读当前单词 → 按 S → 断言跳到下一个不同单词 → 断言被跳过的词后续再次出现 |
| TC-11 | 模式 3 练习中按 Q → 断言 stdout 出现主菜单 |
| TC-16 | 模式 3 → 读当前单词 → 输入正确拼写 → 断言出现"对" → 断言下一词不同 |
| TC-17 | 模式 3 → 读当前单词 → 输入故意错误的拼写 → 断言 stdout 显示正确拼写 → 断言该词后续再次出现 |
| TC-18 | 模式 3 → 正确拼写同一词两次 → 断言该词在 stdout 历史中出现次数 ≤ 2 |
| TC-29 | 按 G → 断言 stdout 含语法点名称 + 编号（预期值取自 §1.5 语法点表） |
| TC-33 | 记录目录 baseline → 跑模式 1 产生录音 → 退出 → 断言无新增 wav/mp3 |
| TC-37 | 第一轮进程：模式 3 使部分词通过 → 记录已通过词列表 → 退出。第二轮全新进程：模式 3 → 断言已通过词重新出现 |

---

# 第三阶段：执行测试，生成测试报告

## 3.1 执行流程

1. 启动被测程序，确认满足第一阶段的 A~H 约定
2. 运行 `test_runner.py`，依次执行 12 条用例
3. 每条用例输出一行 JSON（含用例编号、通过/失败/跳过状态、关键 stdout、断言明细）
4. 所有用例跑完后，`test_runner.py` 汇总 JSON 记录，生成 `test_report.md`

## 3.2 测试报告格式（test_report.md）

每一条用例的格式：

```markdown
## TC-01：启动程序 —— ✅ 通过 / ❌ 失败 / ⏭️ 跳过

- **测试内容**：（从测试用例文档原文引用）
- **执行过程**：
  1. 启动子进程 `python main.py`，PID=12345
  2. 等待 stdout 输出，超时=5s
  3. 读取到 N 行输出
- **关键输出片段**：
  ```
  （粘贴程序实际 stdout 的前 10 行）
  ```
- **断言结果**：
  | 断言 | 依据（文档出处） | 结果 |
  |------|-----------------|------|
  | stdout 包含 "西班牙语陪练" | TC-01 预期结果第1条 | ✅ |
  | stdout 包含 "1"~"5" 和 "G"、"Q" | TC-01 预期结果第2条 | ✅ |
  | 进程未退出，仍在等待输入 | TC-01 预期结果第3条 | ✅ |
- **代码依据**：断言代码见 `test_tc01.py:15-25`，每行断言上方有注释标注对应的测试用例条款号
```

报告末尾的汇总：

```markdown
## 汇总

| 用例 | 结果 |
|------|------|
| TC-01 | ✅ |
| TC-02 | ✅ |
| ... | ... |
| TC-37 | ✅ |

通过：X / 12，失败：Y / 12，跳过：Z / 12

---

## 防止造假机制

| 机制 | 说明 |
|------|------|
| 断言挂文档 | 每条断言代码的注释必须引用测试用例的具体行号和条款 |
| stdout 原文取证 | 报告中粘贴程序的真实 stdout 输出，不用脚本的"理解"代替 |
| 输入来自输出 | 拼写测试中，脚本输入的内容是从程序 stdout 中读到的，不是脚本自编的 |
| baseline 对比 | 文件残留检查用"前后差异"而非"目录为空" |
| 独立进程验证 | 持久化测试用两个独立进程，防止共享内存状态导致的虚假通过 |
| 预期值来自文档 | 语法点清单等预期值直接取自 §1.5 的生词表/语法点表，不动态生成 |
```

## 3.3 跳过策略

| 情况 | 处理 |
|------|------|
| 程序未实现某模式（如无 G 键功能） | 依赖该模式的用例标记为"跳过"，注明原因 |
| 程序 stdout 不满足 A~H 约定 | 受影响的用例标记为"跳过"，注明缺失哪条约定 |
| 环境无麦克风（TC-33） | TC-33 标记为"跳过"，注明"无音频设备" |
| 程序 crash | 当前及后续依赖用例标记为"失败"，附 crash 堆栈 |

---

# 实施顺序

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | 将 §1.5 的教材数据写死进程序，按需求规格和交互设计编写程序 | `main.py` |
| 2 | 确认程序满足 stdout 约定 A~H | 可进入测试阶段 |
| 3 | 将 §1.5 的生词表/语法点表硬编码进测试脚本作为预期值 | `conftest.py` |
| 4 | 编写 conftest.py（进程管理 + 断言工具） | `conftest.py` |
| 5 | 编写 7 个测试模块 | `test_tc*.py` |
| 6 | 编写 test_runner.py（编排 + 报告生成） | `test_runner.py` |
| 7 | 执行测试，生成 test_report.md | **最终交付物** |
