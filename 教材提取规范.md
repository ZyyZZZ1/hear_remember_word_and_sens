# 教材提取规范

> 本文档供 LLM Agent 和人类协作者阅读，用于将「原生教材」逐课提取为「教材」。
> 所有规则用中文描述，Agent 执行时逐条遵守，不得跳过。

---

## 一、任务目标

把 `原生教材/` 文件夹里的课程文件（如 `01-15.txt`），按照统一格式转换为 `教材/` 文件夹下的同名文件。

**如果 `教材/` 下已经存在同名文件，则跳过该课，不要覆盖。**

---

## 二、输入与输出

### 输入
- `原生教材/` 下按编号命名的 `.txt` 文件，从最小的编号开始处理
- 已处理过的所有 `教材/*.txt`（用于判断生词）

### 输出
- `教材/` 下同名 `.txt` 文件，UTF-8 编码，三段式结构

---

## 三、输出格式（严格遵守）

```
# 生词
西班牙语	中文翻译
西班牙语	中文翻译
...

# 例句
西班牙语句子	中文翻译
西班牙语句子	中文翻译
...

# 语法点
语法点标题
详细解释（可多行）
对应例句：数字,数字
（语法点之间空一行分隔）
```

**格式要点：**
- 西语和中文之间用 **Tab** 分隔，不是空格
- 生词一行一个，短语/固定搭配也放在生词区
- 例句按原文出现顺序排列，去重
- 语法点之间空一行
- `对应例句` 用逗号分隔的数字（1-based，即例句区的行号）

---

## 四、生词提取规则

### 4.1 核心定义
> **生词 = 在编号比它小的所有课程中均未出现过的词或固定短语。**

### 4.2 判断方法（逐课累积）
1. 处理第一课（如 01-04）时，没有任何已学词汇，**所有词都是生词**。
2. 处理后续课程时，需要把前面所有课程的「生词区」里的西班牙语条目收集起来，作为「已学词汇库」。
3. 对于本课原文中出现的每个词/短语，先在已学词汇库里检查：
   - 检查**原形**（动词查不定式，名词查单数，形容词查阳性单数）
   - 如果原形已学，则本课不算生词
   - 如果原形未学，则列入本课生词

### 4.3 动词的处理
- 生词区里**必须包含动词原形（不定式）**，如 `vivir`、`hablar`
- 动词的**各人称变位形式也算生词**，但如果原形已学、仅变位形式不同，可不再重复列原形
- 如果本课首次出现该动词的某个变位（如 `vive`），而该动词原形（`vivir`）在前序课程从未出现，则原形和变位形式**都算生词**
- **自复动词（代词式动词）必须以带自复代词的形式列为生词**，如 `levantarse`（而非 `levantar`）、`peinarse`（而非 `peinar`）、`irse`（而非 `ir`）

### 4.3.1 动词变位标注（强制）
- **每个动词生词必须在中文翻译后标注完整的陈述式现在时变位**（6个人称：yo, tú, él/ella/usted, nosotros, vosotros, ellos/ellas/ustedes）
- 格式：`动词	中文翻译 [变位1, 变位2, 变位3, 变位4, 变位5, 变位6]`
- 自复动词的变位必须包含自复代词：`levantarse	起床 [me levanto, te levantas, se levanta, nos levantamos, os levantáis, se levantan]`
- 变位之间用逗号+空格分隔
- 示例：
  - `hablar	说，讲 [hablo, hablas, habla, hablamos, habláis, hablan]`
  - `tener	有 [tengo, tienes, tiene, tenemos, tenéis, tienen]`
  - `levantarse	起床 [me levanto, te levantas, se levanta, nos levantamos, os levantáis, se levantan]`

### 4.4 固定搭配
- **2 个及以上单词**构成的固定表达，放在生词区
- 即使组成单词均已学过，只要这个组合是新的固定用法，就列入
- 示例：`todos los días`、`por la mañana`、`después de`、`todo el día`

### 4.5 人名和地名
- **人名不列入生词**（如 Juan、Ana、Paco 等）
- **地名和国名需要列入**（如 Lima、Perú、Cuba、Alemania 等）

### 4.6 一词多义/多词性
- 如果同一个词在之前的课里出现过，但本课使用的是**不同的含义或词性**，则应当再次列入生词
- 示例：`tarde` 在 01-09 作为名词「下午」已学，但在 01-14 作为副词「晚、迟」是新义 → 列入
- 示例：`estudio` 在 01-07 作为动词变位「（我）学习」已学，但在 01-14 作为名词「学习/书房」是新词性 → 列入

### 4.7 纯语法课的处理
- 如果整课是语法总结（如物主形容词体系化、介词用法总结），且所有单词均在前序课程出现过，则生词区标注 `（本课无新增生词）`
- 不要为了凑数把已学词汇重新列一遍

---

## 五、例句提取规则

### 5.1 提取范围
- 提取原文中**每一个完整的西班牙语句子**
- 包括：
  - 对话里的每一句完整话
  - 叙述性文字里的完整句子
  - 语法例句（变位表格旁边的例句、tener 例句等）
  - 原文中带中文翻译的句子
- **不遗漏原文任何一句完整西语**

### 5.2 去重
- 同一个句子如果原文出现多次，例句区只保留一次

### 5.3 排序
- 按原文出现顺序排列
- 如果需要在例句区补充对比例句（如语法讲解需要的 S-V / V-S 对比例句），追加在末尾，且用词必须**不超过本课词汇范围**

### 5.4 翻译
- 原文自带翻译的，直接使用
- 原文没有翻译的，自行配上准确的中文翻译

### 5.5 断句标准
- 以 `.`、`?`、`!` 作为句子结束标记
- 分号 `;` 或逗号连接的复合句，如果两部分能独立成句则分开处理，否则合并

---

## 六、语法点提取规则

### 6.1 来源
1. **原生教材自带的语法讲解** —— 原文中的编号语法点（如「1. 形容词的阴阳性变化…」）
2. **从生词/例句中提炼的新语法现象** —— 原文没有明确编号讲解，但本课首次出现的语法结构

### 6.2 写法要求
- 每个语法点包含：
  - **标题**：简洁概括（如「规则动词变位总述」「疑问句中的主语位置」）
  - **正文**：用中文详细解释，包含规律、用法、注意事项
  - **对应例句**：标出例句区里能佐证此语法点的句子序号（逗号分隔）

### 6.3 系统性
- 如果本课的语法点和前序课程有关联，应当在正文中**跨课引用**
- 示例：「与疑问句中的主语后置不同（疑问句必须后置，见 01-06），陈述句的主语后置是可选的」

### 6.4 例句编号准确性
- `对应例句` 里的数字必须准确对应例句区的行号（从 1 开始）
- 如果修改了例句区（新增/删除），必须同步更新所有语法点的对应编号

### 6.5 不要遗漏
常见容易遗漏的语法点类型：
- 主语/动词的语序变化（疑问句动词前置、陈述句主语后置等）
- 新出现的虚词用法（连词 `que` 表示比较、介词的新义项等）
- 同根词的词性转换（动词 → 名词等）

---

## 七、执行流程（Agent 必须遵循）

### 第一次执行（初始化）
1. 列出 `原生教材/` 和 `教材/` 的内容
2. 确定哪些课需要生成（= 原生有但教材没有的）
3. **按编号从小到大排序**

### 逐课处理
1. 读取本课原生教材内容
2. 汇总前面所有课程（含已生成的）的「生词区」条目 → 构成「已学词汇库」
3. 根据本文第四~六章的规则，逐条提取生词、例句、语法点
4. 生词在列入之前，逐一与已学词汇库比对，确认是真正的新词
5. 写文件到 `教材/` 目录
6. 进入下一课

### 注意事项
- **不要用脚本硬解析**，必须用 LLM 逐课分析生成
- 每课必须独立判断，禁止模板填空
- 生成后自查：例句是否覆盖了原文所有句子？生词是否和前面重复？

---

## 八、质量自查清单（每课生成后必查）

- [ ] 原文第一段有没有句子被漏掉？（常见错误：跳过大段叙述直接处理对话）
- [ ] 生词里有没有人名？
- [ ] 生词里有没有和前面课程重复的词？
- [ ] 固定搭配（2 词以上）有没有被遗漏？
- [ ] 例句的翻译齐全了吗？
- [ ] 语法点的 `对应例句` 编号对不对？
- [ ] 西语和中文之间用的是 Tab 而不是空格？
- [ ] 文件编码是不是 UTF-8？

---

## 九、常见错误与纠正

| 错误 | 纠正 |
|------|------|
| 只处理对话，跳过叙述段 | 叙述里的完整西语句子也要提取为例句 |
| 人名列入生词 | 人名不列入（地名/国名要列入） |
| 把已学词当生词 | 逐词与已学词汇库比对原形 |
| 忘记固定搭配 | 扫一遍原文，2 词以上的固定表达全部提取 |
| 语法点例句编号写错 | 生成后从 1 开始数一遍例句，确保编号对上 |
| 西语中文之间用空格 | 必须用 Tab |
