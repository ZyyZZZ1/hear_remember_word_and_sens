# 连读听力方案 v4（最终版）

## 1. 目标

解决"单词认识但放进句子听不出"——根因是西语连读抹掉词界。训练大脑在连读音流里切词界。

## 2. 范围

**不新增模式。** 词界标注作为 feature，挂到现有"听西语句子"场景：

| 入口 | 改动 | 判分键（沿用现有） |
|---|---|---|
| 模式 1 → [2] 句子 | 加难度菜单 + 词界标注 | P/N/B/R/G/F/Q |
| 模式 4 跟读 | 加难度菜单 + 词界标注 | Y/N/S/F/Q |
| 模式 2 → [2] 句子、模式 5 句子题项 | 仅加词界标注（反馈显示用），不挂难度菜单 | 各自原键 |

## 3. 词界标注 —— 定位

**词界版是"按西语连读规则的理论预期标注"，不是"TTS 实际发音的精确转写"。**

- TTS（Piper 神经网络）的连读是模型自学的，不保证 100% 符合教科书规则
- 规则标注对西语连读约 95%+ 命中；剩下 5% 是规则例外或 TTS 个体差异
- 标注与听感冲突时 = 学习机会（按 `W` 拆听可验证 TTS 实际怎么读）
- 不做音频能量谱检测（复杂度暴增且不准）

## 4. 词界标注 —— 显示方式

**默认显示正常句子**（正常空格，无标注）。
**按 `V` toggle 切换词界版**（按一次开、再按一次关）。

**词界版规则**：连读的词直接拼写合一（去空格），不连读的保留空格。**连续连读传递合一成一个块**（不重复中间词）。

### 连读块判定（4 类边界全合一）

| # | 类型 | 条件 | 例 |
|---|---|---|---|
| 1 | 连链 | 辅音尾 + 元音首 | `en una` |
| 2 | 吞音 | 同元音尾 + 同元音首 | `mi hijo`、`de este` |
| 3 | 合音 | 元音尾 + 异元音首 | `lengua en`、`la otra` |
| 4 | 邻接 | 辅音尾 + 辅音首 | `universidad de` |
| — | 不连 | 元音尾 + 辅音首 | `nuestra lengua` |

### 示例（传递合一）

原句：`Enseño nuestra lengua en una universidad de este país.`

边界判定：
- `Enseño|nuestra` 元→辅，不连
- `nuestra|lengua` 元→辅，不连
- `lengua|en` 合音，连
- `en|una` 连链，连
- `una|universidad` 同元吞音，连
- `universidad|de` 辅辅邻接，连
- `de|este` 同元吞音，连
- `este|país` 元→辅，不连

连读块：`[lengua en una]` `[universidad de este]`

```
正常版：  Enseño nuestra lengua en una universidad de este país.
词界版：  Enseño nuestra lenguaenuna universidaddeeste país
```

### 颜色区分

连读块内用颜色交替区分原词边界：块内第一个词绿色、第二个黄色、第三个绿色……

ANSI 颜色：`\033[32m`(绿) `\033[33m`(黄) `\033[0m`(恢复)

```
lenguaenuna → 绿lengua + 黄en + 绿una
universidaddeeste → 绿universidad + 黄de + 绿este
```

> 终端上 `lengua` 绿 + `en` 黄 + `una` 绿，一眼看到词界位置。

### 时机

**听后揭示**：词界版默认在整句连听结束后才允许 `V` 切换（听之前只显示正常版，逼你先自己切词界）。

## 5. 难度菜单（仅模式1句子 + 模式4）

进入句子练习时先选：

```
[1] 听写抓词   —— 连听后敲出抓到的词，程序比对
[2] 纯听       —— 只听+看，自判
[Q] 返回
```

## 6. 流程 —— 难度 [1] 听写抓词（每句循环）

1. **连听**：整句常速 TTS 2 遍（全部 4 模型池，自然连读）
2. **抓词**：键盘敲入听到的词，空格分隔，顺序无关
3. **比对呈现**（见 §7）
4. **揭示**：显示正常句子 + 中文；可 `V` 看词界版
5. **可选拆听**：按 `W` → 逐词朗读（2 个单词安全模型，词间插 ~300ms 静音，无连读）；约 5-10 秒/句，期间不可操作
6. **再连听**：整句常速 1 遍（可开着词界版对照听）
7. **自判**：全对自动判对（pass）；有错自动留队（N），用户可强制按 P/Y 过
8. **按键**：P 或 Y 强制过 / N 确认留 / R 重听整句 / W 拆听 / V 词界 / B 上词(仅模式1) / S 跳过(仅模式4) / F 收藏 / G 下组(仅模式1) / Q 退

## 7. 比对呈现 —— 宽松匹配（尝试功能）

> **定位**：抓词比对是尝试功能，已知会有 bug（顺序、重复词、模糊匹配边界），不好用会删。先做再说。

### 比对规则

- **忽略重音 + 大小写**（`este` = `Este`，`país` = `paIs`）
- **顺序无关**（集合比对）
- **模糊匹配**：用户输入的词和原句里的词，即使拼写有误（少字母/多字母/差一两个字母）也算抓到。用相似度匹配（difflib ratio ≥ 0.8 算同一词）
- **重复词**：原句有两个 `en`，用户打了一个 `en` → 抓到 1 个、漏 1 个

### 多打的处理

用户把连读块当一个词打（如 `lenguaenuna`）→ 原句里没有这个词、模糊匹配也匹配不上 → 算**漏 3（lengua, en, una）+ 多 1（lenguaenuna）**。按正常句子的打分逻辑。

### 显示

```
你的答案：lenguaenuna universidad
原句：    Enseño nuestra lengua en una universidad de este país.

── 抓词结果 ──
  ✓ 抓到 (1)：universidad
  ✗ 漏掉 (8)：Enseño, nuestra, lengua, en, una, de, este, país
  ✗ 多打 (1)：lenguaenuna
  得分：1/9 词
```

### 判定

- **全对（0 漏 0 多）** → 自动判对（pass），进入下一句
- **有漏或有错** → 自动留队（N），显示结果后用户可：
  - 按 P/Y 强制过（觉得自己只是笔误）
  - 按 N 确认留队
  - 按 R 重听 / W 拆听 / V 词界 继续练这句
  - 按 Q 退

## 8. 流程 —— 难度 [2] 纯听（每句循环）

1. **连听**：整句常速 TTS 2 遍
2. **揭示**：显示正常句子 + 中文；可 `V` 看词界版
3. **可选拆听**：按 `W`
4. **再连听**：1 遍
5. **自判**：模式1句子 P/N，模式4 Y/N → 错误循环
6. **按键**：同 §6.8

## 9. 拆听

| 维度 | 情况 |
|---|---|
| 切片问题 | 无。`_PIPER_WORD_SAFE` 白名单保证只用 2 个单词安全模型 |
| 等待 | 10 词约 5-10 秒干等 |
| 音质 | 降级到 2 个安全模型，但拆听要的是"听清孤立发音"非自然度 |
| 定位 | 可选 `W`，用户主动触发 |

> 先做，不好用再优化或去掉。

## 10. 已确认项

- 词界切换：`V` toggle（按一次开/再按一次关）
- 4 类边界全合一
- 拆听保留（先做）
- 颜色：绿/黄交替区分块内原词
- 抓词比对：宽松匹配（模糊+顺序无关+拼错也算对）
- 判定：全对自动 pass，有错自动留队（可强制过）
- 多打：算漏 + 多
- 抓词模式整体是尝试功能，不好用会删
